# 律师工作台 3.0

以破产案件为首个领域的律师工作台，以 skill 包形式交付。律师在案件工作区内推进一个个节点，由节点补全模块，由模块补全图。

本文件是 3.0 的权威词汇表。`knowledge/` 下 2.0 沉淀的资料若使用同名词而含义不同，以本文件为准（见 ADR-0001）。

## 流程

**节点**：
流程的最小单元。每个节点恰有一份文书，每份文书对应一个节点；同时递交的多份文书是同一模块下的多个节点，"同出"不是节点的属性。实物动作（接管、张贴、签收）不是节点，只在某份文书里被载明。节点由对其文书的动作推进：生成、确认。状态只有三个正向态与一个终态：未生成、已生成、已确认、不适用；确认即完成。已确认后重出，节点回到已生成，旧的确认留在历史里。节点另有一个与状态正交的属性：有没有空白模板。
_Avoid_：任务、步骤、小节、文书节点、递交事件、审批、阻塞、待材料

**模块**：
图里若干节点的有序集合。模块的构成（节点归属、拆合、命名、顺序）由律师在案件图上、开发者在领域图上裁定；一个模块是律师会一起宣告不适用的最大一组节点，模块数量是这条判据的结果而不是输入。模块可以从指南和手册产出，也可以由 agent 按案件类型产出，也可以由律师或开发者用自然语言描述产出。
_Avoid_：章节、阶段、模块序号、固定 12 模块、模块定义、模块模板

**图**：
当前案件中节点的全集及其状态，状态包括节点的排序和模块的排序；一张图恰属一个领域。图代表该案件已有的流程；节点和模块的不断补充使图逐渐完整。图也是案件唯一的台账：律师的每个动作是图里对应节点上的一条带来源的条目，agent 永不改动律师的条目。律师不直接读图，读由图派生的视图。
_Avoid_：流程模型、工作流、看板、快照、登记表、节点标记、批准记录、分支适用

**惰性原则**：
节点与模块只在被需要时才创建或补全，不预先铺满整个流程。
_Avoid_：预置全流程、全量初始化

**条目**：
图里某个节点上的一条记录，记一个动作、发生时间、来源与该动作带出的事实（如一次生成产出的文书与审查报告）。条目只增，从不修改或删除；来源由动作类型决定：确认与不适用来自律师，生成来自 agent（律师兜底的那份由 agent 登记，来源记为律师）。节点状态由其条目推出，不另行记录。
_Avoid_：记录行、日志、事件、标记、批注

**回合**：
一个节点的一次出件到拍板：从律师发起生成，到读审查报告后确认或要求重出，恰在一个对话里完成，期间的重出仍在同一回合；之后再改（如按法院反馈）是新回合、新对话。一个对话只服务一个节点，同出的一批文书是几个回合。
_Avoid_：会话、任务、轮次、批次

**派生视图**：
由图生成的文件，图每次变动后整体重算覆盖；有两种形态：供律师阅读的视图，与供程序读取的图存档。它不是台账，不承载任何图里没有的事实；其中的前方来自领域图，标明来源；每个模块与节点也标明来源是领域图还是案件，来源按 id 算出、不存进图；节点的时限同样按 id 从领域图查出。
_Avoid_：快照、看板、报表、台账

**前方**：
领域图里有、案件图里还没有的模块与节点。它不在图里，只在派生视图里以来源为领域图的标记列出，供路由回答"接下来做什么"、供可视化画出整条路。图本身仍只在被需要时补节点。
_Avoid_：待办、未开始节点、预置节点、全流程

**时限**：
领域图节点上可选的一句话，记管理人在该节点上自己要守的法定期限的去案件化原文：有天数的或只有锚点的，连同出处性质；只有「及时」或没有明示期限的节点没有它。它只住在领域图：案件图不存、不拷贝、不改写，派生视图与前方按 id 查出原样带出。基准日与具体截止日是案件事实，住在材料与律师陈述里，图不存；审查报告据此推算截止日。路由原样附上这句话，不据它改变下一任务的顺序。
_Avoid_：期限字段、截止日、deadline、到期提醒、倒计时、日历

**图存档**：
派生视图的机器可读形态：引擎每次写图后覆盖重算的一份文件，载明算好的节点与模块状态、文书版本、每条条目原文，另附前方；供可视化插件读取。它不是另存的快照，没有存档时点；一个工作区一份，不跨案件。
_Avoid_：快照、导出、另存、完整图、存档时点

**领域**：
一套通用模块与节点的名字，如"破产"。案件图在顶层记所属领域的名字，路由据此找到领域图。
_Avoid_：案件类型、业务类型、业务线

**领域图**：
一个领域的、没有条目、不含任何案件内容的图，与案件图同一格式、同一引擎读写。它是模块与节点的定义层：节点从领域图带入案件图时只带默认标题与空白模板属性，时限留在领域图里不随带入拷贝；案件里判为通用的模块与节点经回流写入活图，律师侧与开发侧同一条路；出厂种子不由任何人在会话里写，改由入库更新。随 skill 包内置一份种子，活图住用户主目录，也可独立交付。
_Avoid_：模块定义、节点定义、流程模型、模板图、要件清单

**领域目录**：
一个领域的目录，只有三样：领域图、官方模板原件、通用指引手册原文。分两份：`~/.loo0ng/领域/<领域名>/` 是**活图**，律师逐节点回流写的是它，skill 包升级碰不到；skill 包内 `domain/assets/<领域名>/` 是**出厂种子**，首次起手时活图不存在就从它拷一份，之后只认活图。案件工作区靠起手时记下的绝对路径找到活图。见 ADR-0019。
_Avoid_：知识库、模板库、资料目录、领域包

## 动作

**生成**：
agent 依据材料（可再加过往文书、文书提示词或空白模板之一）产出一版文书及其审查报告的动作，由律师发起。agent 效果不佳时律师自己写一份兜底，律师一句话，agent 登记为已生成。生成永不写入案件工作区里已存在的文书文件，每次都是新版本。文书须过版式门禁才落盘：通过与需人眼落盘，不通过则本次生成失败。
_Avoid_：起草、填充、draft

**确认**：
律师读审查报告后对一版文书拍板的动作，只能由律师发起，agent 永不代做；在办节点的对话里由律师一句话发起，条目原样记下这句话。确认即节点完成。开发者在测试工作区里做确认时以律师身份行事。
_Avoid_：批准、审批、两段式、出示与落盘

**不适用**：
律师宣告某个节点在本案不需要走的动作，节点进入终态；与确认一样由律师一句话发起，条目原样记下这句话。
_Avoid_：分支适用、跳过、删除节点

**初始化**：
案件开始时在当前目录建立案件工作区的动作：创建目录层次，归档收件箱里已有的文件，起手图三选一（空图、整份领域图、开发者交付的定制图），末尾回显起手清单交律师一句话，并把领域目录的位置记进工作区；没有文件时只建空目录与起手图。3.0 不导入既有案件，在办案件也从第一份文书起从零建。
_Avoid_：导入归档、setup、迁移、重导

**归档**：
把收件箱里的文件分到材料、指南、模板的动作，由 agent 独自完成、律师不逐件参与：判断去向归 agent，搬运归代码且不改名、不覆盖、不越界；没有正向证据的文件留在收件箱。不登记、不算指纹、不去重。初始化末尾与生成开场自动做，律师也可一句话要求。归档永不产生律师陈述，律师自己写的文件经归档只是普通材料。
_Avoid_：导入、收件扫描、指纹去重、归档记录、分类裁定

**起手清单**：
初始化末尾 agent 回显的一份清单：从指南提出的雏形，加上收件箱里既有成品的建议登记；一句话拍板整张，说了才写入图。
_Avoid_：初始化报告、迁移表、建议表、导入清单

**回流**：
把案件图里律师长出、判为通用的模块与节点写进领域图的动作，有两条路。**律师侧逐节点**是默认路：办案会话里，某个节点确认之后，若领域图里没有它，agent 提出归属与去案件化标题、律师一句话拍板，写的是律师本机的活图，没有第二双眼。**开发侧批量**做两件事：补机制上线前已确认的历史节点，以及开发者定制一个领域图；它写的也是开发者本机的活图：领域图的构建两侧同一条路，开发者要定制就跟律师一样办一遍。两条路回的都只有图的构成，不回条目、裁定与生成空白模板；用过一次（至少一版已确认文书）才算通用；都没有时点。回流从不写出厂种子，那是入库的事。见 ADR-0019、ADR-0020。
_Avoid_：反哺、沉淀、同步、升级、导出

**入库**：
把开发者本机活图那份领域图送进本仓库、成为出厂种子的动作，一条命令：`sketch.py intake --name <领域名>`。它在仓库里做，是一次 git 提交，经隐私钩子与第二双眼；只送 `领域图.json`，官方模板原件与指引手册原文走普通的 git 添加；拷之前校验，并把这次比种子新增与改动的分栏回显，那是第二双眼之前的第一道人眼过滤。它是一次拷贝、不经图引擎。它与回流是两个动作（回流的终点是活图，入库的起点才是活图），与 skill 包的版本发布也是两回事：一次包发布可以带零次或多次入库。律师永不入库。见 ADR-0020。
_Avoid_：发布、分发、合入、同步、上传

**路由**：
随时可调用的只读地图，可选，永远不是流程中的一步。根据用户提示词回答当前案件处于哪个节点、上一个已完成的任务、接下来的任务（含审批、确认、文书生成等），需要具体能力时指向对应 skill 并给出下一句该打什么。路由不触发 skill，不写图，它的回答不成为图里的状态。
_Avoid_：调度、入口、主命令

## 结构

**编排 skill**：
律师打出其名字才触发、驱动一段流程的 skill，模型永不自行调用；律师面对的入口只有起手、办节点两个，路由是可选的独立件。构成编排层。
_Avoid_：主动 skill、命令 skill、动作脚本

**参考 skill**：
被编排 skill 或模型按需调用的工具类 skill；律师在对话里说一句（陈述、归档、改图）时由模型自行调用落盘并回显。构成参考层。
_Avoid_：被动 skill、工具 skill、脚本

**文书**：
节点对应的那一份对外或对内成品。文书有版本，版本属于文书，不在节点上展示；每一版都以直接可提交为目标。
_Avoid_：文件、产物、工作副本、已批准

**审查报告**：
每一版文书必有的一份伴生文件，与文书一起落盘，缺一即生成失败；面向这一版文书、只对文书负责。载明生成依据、存疑点、待律师裁定或修改之处、版式门禁的结论与披露项、结论的理由（结论为需人眼时开头另有一段须目验清单），另有固定一段时限，写出规则、基准日及其出处与推算出的截止日，没有时限的节点写无明示时限；一切本来会写进文书正文的标注都移到这里，文书本体始终以直接可提交为目标。是律师做确认的依据。引用律师陈述按条（即按文件）并标明性质档，只引现行的。
_Avoid_：审查文档、审查目录、取值台账、要件清单、检查清单

**冻结转换器**：
以该节点的官方模板为版式载体、把 Markdown 最小集转成 DOCX 的那段确定性代码。冻结指它不随每次生成重写，不指它的后端不会变：它有一个被钉住的后端版本，用别的版本不阻断出件，但必须在审查报告里写明这一版用的是哪一个。怎么拿到能跑它的运行环境归 agent，不作规定；装出来的东西只落在临时位置，不进案件工作区、不进仓库。
_Avoid_：生成器、导出器、渲染器、docx 脚本

**版式门禁**：
文书落盘前对其成品做的一次只读检查，基于对 DOCX 的静态检查与版面推算，有真实渲染时以渲染取代推算。结论三档：通过、需人眼、不通过。不通过则本次生成失败、文书不落盘；需人眼件照常落盘。检查项分两类：不通过项是客观可测的版式与结构事故，披露项只写进审查报告、留给律师判断。门禁不改文书，不评判内容；agent 对结论没有写权。缺渲染能力不阻断出件，也不把测不准当作通过。
_Avoid_：校验、检查脚本、QA、预检、静默降级

**需人眼**：
版式门禁三档结论之一：某项判据的推算区间横跨其阈值，门禁测不准，而非测出事故。只可能由页数、空白页、行高三项产生，其余判据恒为二值；真实渲染在场时这一档不存在。需人眼件照常落盘，审查报告开头列出须目验的项名、坐标、推算区间与最坏越界幅度。agent 不得把它改成通过。
_Avoid_：警告、warning、待定、不确定、NOT_VERIFIED、软失败

**空白模板**：
一个节点的文书骨架，来源有三：官方模板、从过往文书剔除案件内容、从材料反推。官方模板是来源为官方的空白模板；案件级空白模板存在案件工作区里，按来源分官方与生成两格。其他案件的成品文书不复制进本案，只以剔除案件内容后的空白模板进入。
_Avoid_：分离空白模板、标准模板、模板库、既有文书

**律师陈述**：
律师本人在对话里录入、作为事实来源的材料，一句话一条、一条一个文件，落盘后只读。性质三档：直接陈述（律师本人是权威来源的事实）、转述（律师转录他人或他处，来源强度低一档）、裁定（律师对本案某个问题的选择，不是事实）；案件级裁定记在这里，不另设台账。正文逐字记律师原话，agent 不润色。律师改口是新增一条并指明取代哪一条，整条取代，旧的一字不动，被取代由新的那条推出。对话里说出的事实先落成陈述才算材料。确认与不适用不是陈述，是图里的条目。
_Avoid_：裁定台账、备注、补值、待决标记、口头事实

**律师**：
在案件工作区里办案、发起确认与不适用的人。开发者是在本仓库里做开发会话的人，做律师动作时即是律师。
_Avoid_：用户、办案人员

**第二双眼**：
开发者以外的一次独立审阅，对象是进入本仓库的 diff，专查案件内容；由 AFK 代理在 PR 里做，隐私钩子不算。只有入库与 `knowledge/` 誊入两条路需要它；回流两条路写的都是包外活图、不进仓库，没有这一道（ADR-0019、ADR-0020）。
_Avoid_：人工复核、审批、隐私钩子

**测试工作区**：
一个由种子在临时目录里生出、材料全为合成、跑完即弃的案件工作区，供开发者验证 skill 与脚本，关票的真实触发也在其中做；里面没有任何真实案件内容，也不存进仓库。开发者在其中做律师动作时即是律师。
_Avoid_：测试案件、fixture 案件、样例案件、示例工作区

**案件工作区**：
仓库外存放单个案件的全部材料、指南、模板、文书与图的目录，会话在其中打开，图文件是它的身份标记。案件工作区永不进入本仓库；工作区之上怎么摆不作规定。
_Avoid_：案件目录、案件材料包、案件仓库、容器

**收件箱**：
案件工作区里等待归档的文件落点，律师拷入文件的唯一入口；空即无积压，留在其中的是 agent 拿不准或同名冲突的文件。
_Avoid_：暂存区、未分类、导入目录

**材料**：
案件工作区里本案的事实来源原件：律师拿到的外来文件，只读、原文件名与相对路径不改、不登记；生成与审查时 agent 直接整读。律师陈述是材料的一种。
_Avoid_：原始材料、附件、归档记录

**指南**：
随案拿到的、本案适用的官方要求文件，存在案件工作区里；生成与审查时作为要求而非事实读取，也是雏形的来源。与领域目录里的通用指引手册是两回事。
_Avoid_：指引手册、规范、要件清单

**雏形**：
agent 从指南提出的一批图里尚没有的模块与节点，随起手清单回显，经律师一句话裁定后才写入图；写入前不是图的一部分。同一机制在开发会话里对通用指引手册跑一次，产出的就是领域图。
_Avoid_：预置流程、自动铺图、流程模型、起手图

**可视化插件**：
读取图存档、把图呈现给律师看的组件；呈现物（HTML 或别的形态）由它产出，图的引擎不产出任何呈现物。跨案件汇总也归它。
_Avoid_：工作台、看板、仪表盘
