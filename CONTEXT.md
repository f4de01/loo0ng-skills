# 律师工作台 3.0

以破产案件为首个领域的律师工作台，以 skill 包形式交付。律师在案件工作区内推进一个个节点，文书出一版、律师填高亮处、一句话确认，图记下每一步。

本文件是 3.0 的权威词汇表。`knowledge/` 下 2.0 沉淀的资料若使用同名词而含义不同，以本文件为准（见 ADR-0001）。2026-09-14 按 ADR-0023 整体改写；同日按 ADR-0024 新增「确定性规则」，「版式门禁」「需人眼」退场。2026-09-16 按 ADR-0029 把「可视化插件」改称「桌面卡片」并整条改写。2026-09-17 按 ADR-0030 补起手与桌面卡片之间那条边。2026-09-19 按 ADR-0031 新增「案件根目录」并锐化「案件工作区」，按 ADR-0032 把起手与桌面卡片那条边由写入改为只读。

## 流程

**节点**：
流程的最小单元。每个节点恰有一份文书，每份文书对应一个节点；同时递交的多份文书是同一模块下的多个节点。实物动作（接管、张贴、签收）不是节点，只在某份文书里被载明。节点由对其文书的动作推进：生成、确认。状态只有三个正向态与一个终态：未生成、已生成、已确认、不适用；确认即完成。已确认后重出，节点回到已生成，旧的确认留在条目里。节点另有一个与状态正交的属性：有没有空白模板。
_Avoid_：任务、步骤、小节、文书节点、递交事件、审批、阻塞、待材料

**模块**：
图里若干节点的有序集合。模块的构成（节点归属、拆合、命名、顺序）由律师在案件图上、开发者在预设图上裁定；一个模块是律师会一起宣告不适用的最大一组节点。模块也是文书目录的第一层。
_Avoid_：章节、阶段、模块序号、固定 12 模块、模块定义、模块模板

**图**：
当前案件中节点的全集及其状态，含节点与模块的排序。图自足：起手之后不再依赖任何图外的东西算状态与前方。图也是案件唯一的台账：律师的每个动作是图里对应节点上的一条带来源的条目，agent 永不改动律师的条目。律师不直接读图，读由图派生的视图。
_Avoid_：流程模型、工作流、看板、快照、登记表、节点标记、批准记录、分支适用、领域图

**惰性原则**：
节点与模块只在被需要时才由律师一句话创建，不预先铺满整个流程；预设图整份带入是律师的选择，不违反它。
_Avoid_：预置全流程、全量初始化

**条目**：
图里某个节点上的一条记录，记一个动作、发生时间、来源与该动作带出的事实（如一次生成产出的文书与审查报告）。条目只增，从不修改或删除；来源由动作类型决定：确认与不适用来自律师，生成来自 agent（律师兜底的那份由 agent 登记，来源记为律师）。节点状态由其条目推出，不另行记录。同一节点重出覆盖同一份文书，条目仍新增一条。
_Avoid_：记录行、日志、事件、标记、批注、版本记录

**回合**：
一个节点从出件到确认的一段，默认在一个对话里完成：律师发起生成、填高亮或补事实要求重出、最后一句确认或不适用。律师在 Word 里填完隔天再开对话说确认也算同一回合：出件与确认可跨对话，但一个对话只服务一个节点。
_Avoid_：会话、任务、轮次、批次

**派生视图**：
由图生成的文件，图每次变动后整体重算覆盖，两种形态：供律师阅读的一份，与供桌面卡片读取的机器可读的一份。它不是台账，不承载任何图里没有的事实；每个节点标明算好的状态、最近一次生成与确认、时限句、高亮是否已清。状态只在引擎里算一次，读它的人不自己算。
_Avoid_：快照、看板、报表、台账、图存档、存档时点

**前方**：
案件图里还没生成的节点，按图序列出，供路由回答"接下来做什么"。已确认与不适用的不在前方。
_Avoid_：待办、未开始节点、预置节点、全流程、领域图里有而案件图没有的

**时限**：
节点上可选的一句话，记管理人在该节点上自己要守的法定期限的去案件化原文，连同出处性质。它由预设图带入案件图，律师不在案件图上写它；本案的基准日与具体截止日是案件事实，住在材料里，审查报告据此推算截止日。路由原样附上这句话，不据它改变顺序。
_Avoid_：期限字段、截止日、deadline、到期提醒、倒计时、日历

**领域**：
一份预设图覆盖的一类案件，如"破产"。它只是预设图的名字所指的东西，不是图的字段，不参与任何计算。
_Avoid_：案件类型、业务类型、业务线、领域名字段

**预设图**：
一份构建完成、没有条目、不含案件内容、带着空白模板的图，起手时整份拷进案件工作区。有两种归属：**出厂**的随 skill 包分发、住包内、升级整个换掉、谁都不在会话里写；**个人**的住律师本机、由律师从案件图另存而来、升级碰不到。两者结构相同，同名不并存。案件工作区只记起手自哪份预设图的名与归属，不记路径。
_Avoid_：领域图、活图、出厂种子、定制图、领域目录、模板图、流程模型、要件清单

**空图**：
起手的另一个选择：图上没有任何模块与节点，律师一句句长出来。给开发者没提供预设图、或律师想自己建图的情形。
_Avoid_：新领域、白图、自定义模式

## 动作

**生成**：
agent 以该节点的空白模板为底、依据材料与同模块在前的已确认文书出一版文书的动作，由律师发起。agent 只改模板上该改的位置（填槽、条件段取舍、删说明段、落律师的要求），材料里没有的槽保留原占位并加高亮，没把握的句子也加高亮；缺事实不停下来问。差量施加成功即覆盖落盘，一节点一份，不留版本；差量被拒则改差量重来，仍被拒则本次生成失败。agent 效果不佳时律师自己写一份兜底，一句话登记为已生成。
_Avoid_：起草、填充、draft、重写、出新版本

**重出**：
同一节点再生成一次的动作。以当前那份文书为底：律师填过的高亮处去高亮、不碰；仍高亮的由 agent 填；律师点名的位置照改。律师的改动默认正确，agent 不碰、不查。agent 靠读当前文书的清单与上一次记下的高亮清单判断哪些已被律师填过，这是判断，不由代码比对。
_Avoid_：新版本、覆盖律师、合并

**高亮**：
文书里标黄的位置：agent 填不了的槽，或 agent 没把握的句子。它的意义只有一个：这里要律师动手。律师在 Word 里填、去不去黄都行；agent 靠比对上一次记下的高亮清单认出哪些已被律师填过。
_Avoid_：待确认标记、批注、占位符、TODO

**确认**：
律师对一节点当前文书拍板的动作，只能由律师一句话发起，agent 永不代做、永不从"高亮已清"推断；条目原样记下这句话。确认即节点完成。可以在任何对话里说，不要求这个对话先出过件。开发者在测试工作区里做确认时以律师身份行事。
_Avoid_：批准、审批、两段式、出示与落盘、自动完成

**不适用**：
律师宣告某个节点在本案不需要走的动作，节点进入终态；与确认一样由律师一句话发起，条目原样记下这句话。
_Avoid_：分支适用、跳过、删除节点

**起手**：
案件开始时在当前目录建立案件工作区的动作：问一件事（空图还是哪份预设图），建目录层次，预设图整份拷入（含时限句与整个模板目录），目录里已有的文件走归档清单，末尾回显起手清单；没有文件要归就没有确认点。一案一次。
_Avoid_：初始化、导入归档、setup、迁移、重导、领域三选一

**归档**：
把待归档里或律师指的任意目录里的文件分到材料、参考/模板、参考/指南的动作。判断去向归 agent，搬运归代码：不改名、不覆盖、按内容去重、维护归档索引。律师一句话发起的整理与起手末尾那次先出清单、律师拍板再搬；生成开场清待归档不问。工作区外的目录只复制，原件不动。拿不准的与同名冲突的留在待归档并说明。
_Avoid_：导入、收件扫描、分类裁定、解压、识图、改名归档

**起手清单**：
起手末尾 agent 回显的一份清单：归档去向、既有成品的建议登记、同名冲突，一句话拍板整张，说了才搬、才写图。
_Avoid_：初始化报告、迁移表、建议表、导入清单、雏形清单

**另存**：
律师把当前案件图存成一份个人预设图的动作：剥掉全部条目与不适用记录，只留模块、节点、空白模板与时限；标题去案件化由 agent 列出改法、律师一句话拍板；模板整份拷走。
_Avoid_：回流、沉淀、导出、同步、升级

**导入**：
开发者从一个来源（目录树、指引手册、别的案件图）长出一份预设图的动作：agent 整读来源写成雏形，经判重与开发者拍板写入个人预设图，再经 PR 进仓库成为出厂预设图。律师不导入。
_Avoid_：入库、回流、批量回流、发布、分发

**雏形**：
agent 从一个来源提出的一批图里尚没有的模块与节点，经判重回显、一句话裁定后才写入图；写入前不是图的一部分。开发者导入用它；律师从空图建图时一句话"从这份指南里提节点"也用它；起手不再跑它。
_Avoid_：预置流程、自动铺图、流程模型、起手图

**路由**：
随时可调用的只读地图，可选，永远不是流程中的一步。回答当前案件处于哪个节点、上一个已完成的、接下来的，以及哪些文书高亮已清但还没确认；需要具体能力时指向对应 skill 并给出下一句该打什么。路由不触发 skill，不写图，它的回答不成为图里的状态。
_Avoid_：调度、入口、主命令

## 结构

**编排 skill**：
律师打出其名字才触发、驱动一段流程的 skill，模型永不自行调用；律师面对的入口只有起手、办节点两个，路由是可选的独立件。构成编排层。
_Avoid_：主动 skill、命令 skill、动作脚本

**参考 skill**：
被编排 skill 或模型按需调用的工具类 skill；律师在对话里说一句（归档、改图、另存）时由模型自行调用落盘并回显。构成参考层。
_Avoid_：被动 skill、工具 skill、脚本

**文书**：
节点对应的那一份对外或对内成品，住在文书目录下该节点的模块与节点两层里，一节点一份，重出覆盖。它就是稿子：agent 在它上面改，律师在它上面填。
_Avoid_：文件、产物、工作副本、已批准、版本、稿子与成品两份

**审查报告**：
每一版文书必有的一份伴生文件，与文书一起落盘，缺一即生成失败。它是机器留痕，不要求律师读：生成依据（每个 agent 填过的槽记一行来源）、高亮清单（位置与原占位，重出时据此认出律师填过的）、施加原话（代码施加差量时的回显：出件环境、逐条改动、留黄、拒改）、时限推算。律师兜底的那份只写一行"律师自写"。
_Avoid_：审查文档、审查目录、取值台账、要件清单、检查清单、律师必读

**差量**：
agent 对一份文书提交的改动集合：按段落或单元格编号写明填什么、删哪段、加或去哪处高亮；由代码在文书原件上原地施加，agent 不重写整篇。施加只落在指定的字符区间上，骨架、表格几何与区间外的格式由构造原样保住，元数据在收尾清掉；差量本身不合法（编号越界、原文不唯一、区间重叠、所在 run 含脚注图片或域）即整件拒改、一个字不写。
_Avoid_：稿子、Markdown 稿、全文、模板重写、门禁、校验

**确定性规则**：
由代码执行、不经模型判断的规则，只有三种：字节必须原样保住的（在 docx 上施加差量、拷贝与搬运不改名）；被检查的一方是模型自己的（差量的拒改、雏形的同名判重）；对律师文件的写入必须确定性可重放的（图的唯一写入口）。其余都是判断，归模型加 skill 正文里的方法与律师的眼睛。一条规则只在真实事故之后进入；事故的教训落不进三种之一的写进正文或 eval 用例。
_Avoid_：门禁、版式门禁、需人眼、校验、机械检查、版面推算、相似度、模型不可靠所以用代码兜

**空白模板**：
一个节点的文书骨架，一份 docx，住在参考/模板下，由图上该节点的空白模板属性按文件名指认。来源可以是官方原件、别案成品剔除案件内容、律师自己的空白件，目录不按来源分格。没有空白模板的节点出不了件。
_Avoid_：分离空白模板、标准模板、模板库、既有文书、官方格与生成格

**律师说过的**：
律师在对话里给出的本案事实与选择，agent 一句一行追加进材料里的一份文件（日期、原话），不判它落没落进文书、不回显。改口是再追加一行，后说的覆盖先说的由整读时判。确认与不适用不是它，是图里的条目。
_Avoid_：律师陈述、裁定台账、备注、补值、口头事实、性质三档

**律师**：
在案件工作区里办案、发起确认与不适用的人。开发者是在本仓库里做开发会话的人，做律师动作时即是律师。
_Avoid_：用户、办案人员

**第二双眼**：
开发者以外的一次独立审阅，对象是进入本仓库的 diff，专查案件内容；由 AFK 代理在 PR 里做，隐私钩子不算。只有导入的预设图进仓库与 `knowledge/` 誊入两条路需要它；另存写的是律师本机、不进仓库，没有这一道。
_Avoid_：人工复核、审批、隐私钩子

**测试工作区**：
一个由种子在临时目录里生出、材料全为合成、跑完即弃的案件工作区，供开发者验证 skill 与脚本；里面没有任何真实案件内容，也不存进仓库。开发者在其中做律师动作时即是律师。
_Avoid_：测试案件、fixture 案件、样例案件、示例工作区

**案件工作区**：
仓库外存放单个案件的全部材料、参考、文书与图的目录，就是律师开会话的项目目录，一项目一案，图文件是它的身份标记。它由律师在客户端里新建项目时确定，位置因此天然落在会话的工作目录上；客户端只认自己建的项目，所以把一个已有的工作区接进来是"先新建项目再把内容搬进去"，不是"搬过去再打开"。整个目录可以无损移动：图、指针块与归档索引里记的一律是相对工作区根的路径。案件工作区永不进入本仓库。
_Avoid_：案件目录、案件材料包、案件仓库、容器、子目录工作区

**案件根目录**：
装着若干个案件工作区的那一层目录，**桌面卡片**要扫的就是它，底下一层有派生视图机器可读形态的目录就算一个案件。它是一台机器上的一项设置，不属于任何一个案件，也不在任何案件工作区里。**起手**不许落在案件根目录本身：那样建出来的案件卡片永远扫不到，而起手仍会报成功。
_Avoid_：案件库、案件文件夹、工作区父目录、扫描路径、案件清单

**待归档**：
案件工作区里等待归档的文件落点；空即无积压，留在其中的是 agent 拿不准或同名冲突的文件。起手前律师直接丢在工作区根的文件同样视为待归档。
_Avoid_：收件箱、暂存区、未分类、导入目录

**材料**：
案件工作区里本案的事实来源原件：律师拿到的外来文件，只读、原文件名不改；归档可按主题建子目录；生成与审查时 agent 整读。律师说过的那份文件是材料的一种。
_Avoid_：原始材料、附件、归档记录、按模块分的材料

**归档索引**：
归档维护的一份清单，一行一件：相对路径、一句话是什么、归档日期、有没有转出的文本。生成开场先读它再决定读哪些原件。
_Avoid_：登记表、指纹表、目录树、材料清单

**参考**：
案件工作区里非事实的两类文件：**模板**是空白模板，**指南**是本案适用的官方要求文件，各一格。指南作为要求而非事实读取，也是雏形的来源。
_Avoid_：指引手册、规范、要件清单、资料、知识库

**桌面卡片**：
独立于 Codex 与 Claude Code 的一个应用，以卡片的形态常驻在律师桌面上，同时看住在办的多个案件。它寄居在桌面组件宿主里，只是一个自有组件包。发现案件靠扫律师指定的根目录：底下有派生视图的机器可读形态的目录就算一个案件。它只读不写，且只读那一个文件，工作区里其余各格一律不读不列不显示。展示的内容由它对算好的状态做聚合得出（计数、排序、取前方第一个），判定一概不做：要看条目才能得出的结论，必须引擎先算好。
要扫哪几个**案件根目录**记在律师本机的一份个人设置里，不在案件工作区里，也不在本仓库里。维护那份设置是律师和他的 agent 的事：**起手**只读它、比对一次、说一句这一案卡片看不看得见，从不写它。
_Avoid_：可视化插件、插件、小组件、widget、工作台、看板、仪表盘、导出的 HTML、终端插件、对话里的一页

## Relationships

- 一个**模块**包含若干有序的**节点**，每个节点归属一个模块。
- **图**包含本案全部**模块**与**节点**，并记录它们的排序。
- 每个**节点**恰有一份**文书**，每份文书对应一个节点。
- **条目**归属于一个**节点**，只追加；节点状态由其条目推出。
- **图**是案件唯一的台账，**派生视图**在图每次变动后整体重算，不承载图外事实。
- **派生视图**有供**律师**阅读与供**桌面卡片**读取的两种形态，卡片只读算好的状态。
- **案件工作区**住在某个**案件根目录**下面一层；**桌面卡片**扫根目录来发现案件。
- **起手**只读**桌面卡片**的根目录设置：落在根目录本身就拒绝，上级目录不在列表里就说一句该怎么加；那份设置不在、读不出、形状不对的机器上这一步整步不做。
- **预设图**在**起手**时把模块、节点、时限与空白模板整份带入案件的**图**，此后案件图自足。
- **另存**把案件的**图**剥去条目与案件内容，连同空白模板保存为个人**预设图**。

## Flagged ambiguities

- 「领域图」曾同时指包内的出厂种子与包外累计的活图：ADR-0019 将活图搬出包，ADR-0020 将两侧构建统一到活图并禁止会话写包内种子。已裁定（ADR-0023）：统一称**预设图**，分出厂与个人两种归属，同名不并存；起手后案件的**图**自足，复用走整份**另存**，不再逐节点回流。
- 「插件」曾同时指本仓库分发的 Claude Code 插件与桌面上那个看图的应用。已裁定（ADR-0029）：桌面上那个改称**桌面卡片**，「插件」今后只指 Claude Code 插件；旧 ADR 与 `docs/` 里的「可视化插件」不回头改（ADR-0022）。
- 「包外的家」（`~/.loo0ng/` 下那些跨案件的个人文件）在开发机上是一个随便写的目录，在律师机上不是：那里的 agent 跑在只许写工作目录的沙箱里。已裁定（ADR-0032）：**起手**对这类文件只读不写；由律师一句话发起、写失败会冒泡成一句拒绝的动作（如**另存**）仍可以写。新写要动 `~/.loo0ng/` 的代码，先判这一条：这次写入是静默收尾，还是律师看得见的一个动作。
- 「存档／图存档」曾混指趋近完整时保存的图与供可视化读取的数据。已裁定（ADR-0011、ADR-0023）：供可视化读取的是**派生视图**的机器可读形态，随图变动重算，没有存档时点；案件唯一的台账是**图**，要跨案复用则**另存**为个人**预设图**。
