---
"loo0ng-skills": patch
---

**剪的是两件编排 skill 写给模型看的正文，你的打法一处不变。**

`ask-loo0ng`（问路）与 `doit`（办节点）是全仓最长的两件正文，每次触发都整篇进上下文。这一版逐句过了一遍：同一个意思在两处说的合并到一处，模型本来就会照做的整句删掉，靠「不要做 X」来防的几处改成正面说它该做什么。问路的正文从 160 行剪到 146 行，办节点从 156 行剪到 154 行。

两件的行为一处没改：问路答你的那一行加五段、四问的顺序、办节点那八步的死顺序与收尾三段，全是原来的样子。已经起手的案件工作区不受影响：图、两份视图、文书、归档索引一字不动。

删掉的东西里只有一样你可能用得上：Codex 客户端里打 `doi` 就能补全到 `doit` 这条提示。它没有丢，只是往后只在交付现场清单里维护那一份。
