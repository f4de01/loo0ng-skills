---
"loo0ng-skills": patch
---

to-docx 的版式门禁**整件退场**（#22，ADR-0024）：`scripts/gate.py` 连同版面推算、渲染层、三档结论、退出码 3、阈值旗、`--no-render`、`--template`、`--deliver`、`--overwrite` 全部删除，`tests/to-docx` 里门禁与推算的测试及三件推算夹具一并删。确定性规则只剩三种（字节原样保住、被检查的是模型自己、对律师文件的写入可重放），其余都是判断：撑列、页数、空白页归律师在 WPS 里的眼睛。

`fill.py` 改三处施加构造，不是检查：`delete` 正文最后一张表之后仅剩的末段改清空不删（表直接接 sectPr 的守法从门禁搬进来，与单元格末段、带 sectPr 的段同款）；`fill` 空串按「没填」处理，槽留原占位由收尾留黄，不删 run、不拒改（`replace` 空串照旧是条件块取舍）；`apply --out` 可直接写 `文书/` 下的路径并覆盖，重出覆盖同一份、载体与产物可以是同一路径，拒改时已有文书一个字不动。收尾无条件清元数据留着并有测试。

正文随之重写：出一版四步是打清单、写差量、施加并落盘、回显抄进审查报告；审查报告第三段由「版式门禁」改为「施加原话」，须目验清单那一节删；「把这份 docx 检一下」那个用法消失，「这份模板有哪些槽」留；PyMuPDF 与「通用裁定台账」的字样删干净。19 件官方模板逐件回归照旧全绿，并多守一条元数据清掉。
