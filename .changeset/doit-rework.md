---
"loo0ng-skills": patch
---

`doit` 按 ADR-0022 的单子重做了一遍，名字与打法不变，四处律师感知得到：**稿子改成照模板填，不再照模板重写**，官方模板的文本就是稿子的骨架，材料里没有的案号、日期、金额留空位、句子原样保留并列进审查报告的待律师裁定，模板里有的句子不改写、没有的不添；**没有空白模板的节点本版出不了 DOCX**，出一版会停下来只报缺什么与模板可以从哪来，图一字不动；出一版整读时几份能一次读的一次读进来，往返少了；确认之后那一问的规矩搬去了 `domain` 的 `references/回流.md`，问法与拍板不变。正文从 20 KB 缩到 11 KB，命令行不再进正文，都在四件参考 skill 里。
