---
"loo0ng-skills": patch
---

**剪的是写给模型看的那几段介绍，你的打法一个字不变。**

四件说一句话就到的 skill（`filing`、`graph`、`domain`、`to-docx`）各有一段写给模型看的介绍，告诉它什么时候该够到这件 skill。这四段每一轮对话都占着上下文，却各自把「谁在流程里调谁」重抄了一遍，四份措辞还不一样。这一版把四段各剪到原来的三分之一上下：只留「什么时候够到它」和一句它是什么。

兄弟关系没有丢，它收进了 `ask-loo0ng` 那张「说一句话就到」的表，新添一列「流程里谁调它」，往后只在这一处维护。

你问「这份模板有哪些槽要填」时，回答的形状这一版写死了：一处一行、`p<编号>#<槽号>` 起头、末尾报总数，不再压成「第几段共几处」那样的概括。你下一句点名要改哪一处，用的就是这个编号。

四件 skill 别的行为一处没改，问路答你的那一行加五段也一处没改。已经起手的案件工作区不受影响：图、两份视图、文书、归档索引一字不动。
