---
"loo0ng-skills": patch
---

**装了桌面卡片的机器，起手完一个新案件不用再去改卡片的设置。** 起手收尾把这个案件的上级目录加进 `~/.loo0ng/卡片设置.json` 的 `根目录` 列表，卡片下一轮扫描就看得见这一案；已经在列表里就一个字不动，那份设置不在（没装卡片）就整步不做、也不在回显里提。读不出、写不进都只说一句，起手照样算完成；不想要这一步就在起手的命令后面加 `--no-card`。

桌面卡片是什么、它读什么、去哪装，README 新增一节「桌面卡片（可选）」。
