---
"loo0ng-skills": patch
---

起手按 ADR-0023 重做，**只问你一件事**：空图，还是哪份预设图（列表分出厂、个人两组）。不再问领域名，也不再三选一。**目录非空不拒**：你在 Codex 建好的项目目录里打一句起手就行，目录里已经堆着的文件与子目录一律挪进 `待归档/`，等归档判去向（点开头的 `.git` 之类不动）。一条命令落下新的目录形状（`待归档/`、`材料/`、`参考/模板/`、`参考/指南/`、`文书/`）、图与两份视图、预设图的空白模板整份拷进 `参考/模板/`；空图起手不拷模板。

**工作区指针块只记四项**：预设图的名与归属、图与视图的文件名、入口名、「本工作区对 skill 仓库只读」。**一条路径都不记**，包一升级绝对路径就死的老毛病没有了；要路径时按名与归属当场解析。目录里原本就有 `AGENTS.md` 的，指针块追加在末尾，你原来写的一字不动。

**起手清单三张表合成一张**：归档去向、建议登记为已生成的既有成品、同名冲突与拿不准的，一次回显、一句话拍板，说了才搬、才写图。**没有文件要归就没有这个确认点**，直接收尾。既有成品登记之后挪进它节点的文书目录，审查报告只有一行「律师自写、未跑门禁」，没有任何节点被自动确认。

**「指南非空跑雏形」那一步删了**：起手不再从指南长节点；你想从某份指南提节点，什么时候说什么时候提。环境自检与安装本版不做，正文留着那一节的位置。
