# In Progress

Beta。故意公开，想听反馈；在毕业到 `productivity/`（或 `engineering/`）之前不进插件、不进根 `README.md`、没有 docs 页，随时可能改或消失。本桶目前为空：ADR-0009 定的是草稿放分支不放目录，这个桶留给需要公开试用的那种草稿。

插件装不到这里的 skill，要试就单件装：

```bash
npx skills@latest add f4de01/lawyer-workbench-v3 --skill=<name>
```
