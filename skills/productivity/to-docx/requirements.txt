# 冻结转换器（scripts/md2docx.py）唯一的第三方依赖，随 skill 包分发到办案那台机器（ADR-0018）。
# 装法归 agent：系统的 python、uv 管的、现装的都行，只要装出来的东西落在临时位置，不进案件工作区、
# 不进这个目录与本仓库。装完照 SKILL.md 的「跑得动转换器的环境」跑；门禁 scripts/gate.py 本体零第三方依赖
# （ADR-0017），不读这份清单，任何解释器上都跑得动。
#
# 精确钉，不用区间：转换器除公共 API 外还扎在 docx.oxml 那一层，上游对它不作 SemVer 承诺，
# >=1.2.0,<2.0.0 的上界是空头支票。
# 已知可跑范围：1.2.0。将来谁在别的版本上真跑过全套测试，才往这里加一个数。
# Requires-Python >=3.9：律师那台 mac 的 /usr/bin/python3 是 3.9.6，底线由 #57 审定，#61 是栽在它上面
# 的那次（图引擎一处 3.10 的 API 让每一次写图全炸）；这条底线的机械守门在 tests/python-floor/，不靠这份
# 文件生效。
python-docx==1.2.0
