---
name: loo0ng-domain
description: "律师一句话要从本案指南长雏形（按这份指南出雏形、从指南里提节点、指南里还有哪些没建）、起手末尾归档后 指南/ 非空要出起手清单里的雏形、或要找官方模板原件、指引手册原文、领域图在哪时用；办案会话里某个节点确认之后判它在不在领域图里、提归属提案、律师一句话之后逐节点回流进活图，开发会话里对通用指引手册出领域图、开发侧批量回流进开发者自己那份活图，也都用它。它持有领域目录（一个领域一个目录：领域图.json、模板/ 官方模板原件、指引手册/ 指引手册原文，律师那台机上的活图住 ~/.loo0ng/领域/ 下，由 home 在首次起手时从包内 assets/ 的出厂种子拷出，往后 skill 包升级只换种子、碰不到活图）与雏形机制的零依赖 CLI：模型整读指南写成雏形文件，CLI 按标题判重（同名不再提、相似不同名列为待定）、回显清单，律师一句话拍板后才经 skill \"loo0ng-graph\" 的引擎写入。改图的其他构成与追加条目归 skill \"loo0ng-graph\"；收件箱归档与律师陈述归 skill \"loo0ng-filing\"；出一版文书与拍板由 skill \"loo0ng-doit\" 驱动；起手由 skill \"loo0ng-setup-case\" 驱动；问在哪、下一步做什么是 skill \"ask-loo0ng\"。它不含任何领域语义，没有领域图也能从空图起手；未拍板不写图，不读材料，不改律师写过的东西。"
metadata:
  display-name: "loo0ng-domain"
  short-description: "领域目录三样；从指南长雏形，拍板后经引擎写入"
---

# 领域目录与雏形

一个领域的内容单元只有三样（ADR-0004）：领域图、官方模板 docx 原件、指引手册 docx 原文。三样都是原件或图，没有摘要层：agent 生成与审查时直接整读原文，不维护要件清单、检查清单、出处指针。本 skill 持有它们，并持有把指南变成图里模块与节点的唯一机制：雏形。CLI 是本目录的 `scripts/sketch.py`（Python 标准库，零依赖，不 import 图引擎）。

## 领域目录：活图与出厂种子

同一个领域有两份，结构一样，分工不一样（ADR-0019）：

```
~/.loo0ng/领域/<领域名>/      # 活图：律师这台机上的那一份，跨案件累计，skill 包升级碰不到
<本目录>/assets/<领域名>/     # 出厂种子：随 skill 包分发，升级时整个被换掉
├── 领域图.json     # 与案件图同格式、无条目、不含案件内容；节点可带一句话「时限」
├── 模板/           # 官方模板原件，文件名即空白模板属性里的「文件」
└── 指引手册/       # 通用指引手册原文
```

- **取活图路径**：`python <本目录>/scripts/sketch.py home --name <领域名>`。活图不在就从出厂种子整份拷一份，已经在就一个字不动，回显的第一行是绝对路径（`活图：<路径>`）。活图与种子都没有就拒，那个领域从空图起手。默认位置 `~/.loo0ng/领域/`（跨案件，不挂在任何案件根下）；环境变量 `LOO0NG_HOME` 换「家」那一层，脚本层单测与 eval 用它，律师那台机上不设。
- **造一个全新领域**（开发侧，ADR-0020）：`home --empty --name <领域名>` 在活图位置建目录、经引擎起一份空领域图，回显同样是那一行绝对路径。它只给「活图与出厂种子都没有」的那个领域用，包里已经有种子时拒（不带 `--empty` 再跑一次就是从种子整份拷一份）。`模板/` 与 `指引手册/` 不由它建，原件自己放进去。
- 起手（skill "loo0ng-setup-case"）不用先跑这一条：它的 `init --domain-name <领域名>` 自己子进程调 `home` 拿路径，再把它记进工作区 `AGENTS.md`。**案件工作区找领域目录只看那一行绝对路径**；没有那一行就让律师跑 skill "loo0ng-setup-case"。引擎 `--domain` 收这个目录或其中的 `领域图.json`。0.1.0 起手的工作区那一行指着出厂种子，本版不迁移（要改就手改那一行），处置见 skill "loo0ng-setup-case" 的正文；换完那一行之后此前已确认的节点不会自动补上，补法见 [references/回流.md](references/回流.md)。
- 内置的领域是「破产」，`assets/破产/` 是它的出厂种子：19 件官方模板与 2 份指引手册就位；领域图的 12 个模块、72 个节点由开发会话对这两份手册跑一次雏形长出（#30）：一个节点恰是一份文书（CONTEXT.md「节点」），19 件官方模板各挂在一个节点上，24 个节点带时限句。整份起手即这 12 个模块、前方为空。第一次在这个领域上起手时这 22 件（约 540 KB）整份拷进活图，一次性，零点几秒。
- 模板与手册是 docx，harness 读不了：`python <本目录>/scripts/sketch.py docx-text <文件.docx>` 把正文打成纯文本（段落一行，表格一行一行、格间 `|`），整读它。
- 这三样之外什么都不放：既有案件的 `.doc`、生成空白模板、摘要与登记表都不进（硬边界 1，AGENTS.md 结构不变量 5）。

## 雏形：一个机制，三种来源

| 来源 | 在哪个会话 | 对着哪张图 | 谁拍板 | 产出 |
| --- | --- | --- | --- | --- |
| 案件级指南（工作区 `指南/`） | 律师会话，起手末尾或律师一句话 | 案件图 `图.json` | 律师 | 起手清单里的雏形，进案件图 |
| 通用指引手册（领域目录 `指引手册/`） | 开发会话 | 活图那份领域图 `--kind domain` | 开发者 | 活图多出模块与节点 |
| 案件图（开发侧批量回流，ADR-0012） | 开发会话 | 活图那份领域图 `--kind domain` | 开发者 | 活图多出通用节点；步骤见 [references/回流.md](references/回流.md) |

律师侧逐节点回流不在这张表里：它一次只有一个节点，不走雏形，见下面的[逐节点回流](#逐节点回流律师侧那条路)。

后两行那两条路写的都是**活图**那份 `领域图.json`（ADR-0020：包内出厂种子谁都不许在会话里写，引擎无条件拒）：先跑 `sketch.py home --name <领域名>` 取路径，第 3、4 步的 `--graph` 指它、并加 `--kind domain`。

四步，来源不同只换第一步读什么：

1. **整读来源**。指南或手册的每一份都整读，docx 用 `docx-text`。只从来源提，不从材料、不从常识铺节点：惰性只约束 agent，来源里没写的不提。
2. **写雏形文件**。按 [references/雏形格式.md](references/雏形格式.md) 写一份 JSON：每个模块带标题，每个节点带模块归属、默认标题、顺序（数组序）、空白模板属性（挂哪件官方模板，无则「无」）、时限句（规则见 [references/时限句.md](references/时限句.md)，没有就不写这个键）。写在 `%TEMP%` 下，**不落进工作区**：工作区里只有拍过板的东西。
3. **判重并回显**。`check` 只读，把清单原样读给律师：

   ```
   python <本目录>/scripts/sketch.py check --proposal <雏形.json> [--graph 图.json] [--domain <领域目录>]
   ```

   同名的（归一化后相同：不计空白、标点、全半角）已在图里，不重复提出；相似不同名的列进「待定」，让律师定是同一个还是新的；与领域图同名的标为「按领域图带入」，写入时沿用领域图的 id。清单之外不加自己的判断，也不先写再问。
4. **拍板后写入**。律师一句话（「照这个建」「都要」「第二个不要」）之后才跑 `apply`；起手时这句话是对整张起手清单说的。

   ```
   python <本目录>/scripts/sketch.py apply --proposal <雏形.json> [--graph 图.json] [--domain <领域目录>] [--as-new <标题>]...
   ```

   待定的按律师的话处理：是同一个就从雏形文件里删掉那条，是新的就 `--as-new <标题>`；有一条待定没定 `apply` 就整个拒、一字不写。写入逐条经引擎（默认取兄弟 skill "loo0ng-graph" 的 `scripts/graph.py`，装在别处用 `--engine` 指过去），引擎的回显照读给律师；引擎拒了哪条就照实转告，其余照写。案件图上时限句只回显不写（ADR-0016）：通用的由开发者回流时写进领域图，本案的具体日期是律师陈述，调用 skill "loo0ng-filing"。

## 判重与时限的规矩

- 判重只按标题，节点对节点、模块对模块；相似的判据是一方包含另一方，或归一化后相似度过阈值。阈值由脚本定，模型不调；律师说「这两个是一个」就删条，不改脚本。
- 雏形里的节点只有引擎认的四样：标题、空白模板、时限、（领域图上的）id。要件、出处指针、检查项都不进图（ADR-0004），生成与审查时读原文。
- 时限只记管理人自己在该节点上要守的、有天数或只有锚点的期限，去案件化原文含出处性质；「及时」与无明示期限不记；两源冲突同一句里两条都写。

## 领域图：谁在写哪一份

领域图用同一引擎，加 `--kind domain`，可给稳定可读的 `id` 与时限句。两份里只有一份写得进去（ADR-0019、ADR-0020）：

- **活图** `~/.loo0ng/领域/<领域名>/领域图.json` 是唯一写得进去的那一份，**两侧都写它**（ADR-0020）：律师在办案会话里逐节点长出来，开发者对通用指引手册跑雏形、开发侧批量回流，写的也是自己那份活图。它在 skill 包与本仓库之外，硬边界 2 管不到它。`home` 在首次起手时从种子拷一份，之后只认活图。
- **出厂种子** `assets/<领域名>/领域图.json` 谁都不许在会话里写，引擎无条件拒、没有放行口（ADR-0020）：包一升级它就被整个换掉，写进去的东西静默消失。它改由**入库**更新（`sketch.py intake`，见下面[入库](#入库活图那份领域图进仓库成为出厂种子)：活图那份 `领域图.json` 进仓库，一次 git 提交，走分支 + PR，隐私钩子在 main 上守它，ADR-0014，第二双眼守的就是这一次写）；读它照旧，起手拿它当领域图来源不受影响。

## 入库：活图那份领域图进仓库成为出厂种子

开发者造或维护一个领域，走的是律师那条路（ADR-0020）：`home --empty` 起头，办一遍案子、逐节点回流进**自己那份活图**，攒够了再入库。入库是这条路的收尾，**只在本仓库里做**，一条命令：

```
python <本目录>/scripts/sketch.py intake --name <领域名>
```

- **只拿 `领域图.json`**，不拿整个领域目录：官方模板原件与指引手册原文是原件，进仓库走普通的 git 添加，不绕活图走一圈；整份拷会把活图目录下的任何东西卷进仓库，正是硬边界 1 最不想要的形状。`模板/` 与 `指引手册/` 这一条一个字不动。
- **拷之前先校验**（经引擎 `validate`，那是读不是写），**再把这次比种子多了什么、改了什么分栏回显**：新增、改名、改属性、改归属、种子里有而活图里没有，各占一栏，头一行数出「多了几个模块几个节点、改了几个标题」。两侧同一条路带来一个原来没有的风险：活图里混着开发者试办案子回流进去的东西，入库是整份送进去，第二双眼要审的 diff 一下子变大，这几行就是隐私钩子与第二双眼之前的第一道人眼过滤。念给人听，别跳过。
- **入库不是包发布**：这一条只把文件放进工作树，接着照常走分支、commit、PR。**入库须经第二双眼**（硬边界 2）：审的就是上面那段 diff，隐私钩子在 main 上守着（ADR-0014）。skill 包的版本发布是另一件事（ADR-0009「维护即发布」，changeset + semver），一次包发布可以带零次或多次入库。子命令因此不叫 `publish`。
- **它不经图引擎**：入库是一次拷贝。引擎那条「包内出厂种子谁都不许写」照旧管着会话里的每一次写图，没有放行口，入库没有绕过它，只是走的另一条路：一次要人审的 git 提交。
- 律师那一侧永远不入库：装好的那份 skill 包不在仓库里，这条命令在那里拒。

## 逐节点回流：律师侧那条路

回流是一个词、两条路（ADR-0019），本 skill 是这个机制的主人，两条路的规矩都写在 [references/回流.md](references/回流.md)。律师侧那条**不走雏形**：一次只有一个节点，不写雏形文件、不跑 `check` / `apply`，直接经引擎写活图。它由 skill "loo0ng-doit" 在律师确认文书之后驱动，缺失判定与那一问的写法在它的正文里。要点四条：

- **只对领域图里没有的节点问。** 判定是按 id 的反向减法，引擎已经算好了：`图视图.md` 里这个节点那一行的「来源」是「领域图」就一个字不问，是「案件」才问。问的次数靠这一条随累计衰减到零。
- **两个默认值**：模块继承它在案件图里所在的那个，标题取去掉当事人、案号、金额、日期之后仍成立的写法（与开发侧那套改写同一个口径）。不问时限：律师侧新增的节点没有时限属性，缺省，由开发者补。
- **未拍板不写**，与雏形机制同一条规矩；写入只经引擎，不新增 CLI。
- **没有第二双眼。** 活图不进本仓库，去案件化在这条路上是可用性问题（别让 A 案当事人的名字出现在 B 案的图里），不是隐私门禁；第二双眼守的是**入库**那一次写（活图那份进仓库成为出厂种子），不在回流这一刻（ADR-0012 的 2026-09-10 附注）。

## 与邻居的边界

- 雏形写入之外的一切改图（改标题、调顺序、移动、不适用、追加条目）调用 skill "loo0ng-graph"；本 skill 不直接改 `图.json`。
- 指南进 `指南/` 是归档，调用 skill "loo0ng-filing"；本 skill 只读它。
- 起手时官方模板拷进工作区 `模板/官方/` 由 skill "loo0ng-setup-case" 做；出件读它由 skill "loo0ng-to-docx" 做；本 skill 只是原件的家。
- 本 skill 不含领域语义：判重、写入、读 docx 都不认「破产」两个字；换一个领域只换 `assets/` 下那份出厂种子与活图里对应的目录，`home` 只按名字找目录。
