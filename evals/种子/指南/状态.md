# 种子「指南」的状态

在种子「图引擎」的状态之上（合成小领域「菜园」整份起手：4 模块 9 节点、都未生成、工作区指针块），`指南/` 里多一份 `种植指南.md`（合成的菜园地块管理要求，本案适用的官方要求文件）。指南里写了一个图里没有的模块「越冬」（节点「覆膜」挂表格「覆膜记录」并带一句时限、「清园」无表格且只说「及时」）、一个已有模块「养护」下图里没有的节点「施追肥」（带一句时限）、以及图里已有的「浇水」（同名，不该再提）。没有与图里相似不同名的标题，一句话拍板时不留待定。不含隐私检查器五类正则能命中的值。

其余五格（`收件箱/`、`材料/律师陈述/`、`模板/官方/`、`模板/生成/`、`文书/`）都在，都是空的；没有律师陈述。供 skill "domain" 的用例：开发者一句「按这份指南出雏形」，断言回显与拍板后的 `图.json`。
