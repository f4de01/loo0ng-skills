# 种子「自加节点」的状态

按真实的破产领域目录**空图起手**，再摆出律师自己动过图的样子：

| 模块 | 节点 | 状态 | 来源 |
| --- | --- | --- | --- |
| 接受指定与报备 | 承诺书 | 已生成 | 领域图（id `n-chengnuoshu`，标题被律师从「管理人承诺书及团队人员」改短） |
| 接受指定与报备 | 补充材料说明 | 未生成 | 案件（律师自加，领域图里没有） |

改标题只改标题，id 不变，所以领域图上那句时限仍按 id 查得出：路由该按**现在的标题**
「承诺书」称呼它、给出的整串也用现在的标题，同时仍附上那句时限。
待确认之后要生成的是案件图里唯一未生成的节点「补充材料说明」（五问的问 2，
案件图里已经有的排在领域图候选前面）。

供 skill "ask-loo0ng" 的用例「律师自加节点与改标题」。

两份视图是最后一次写图后重算的。回放末尾把 `图.json`、`图视图.md`、`图视图.json` 三份的 sha256 写进
工作区根的 `.基线.json`（`evals/共用/基线.py`），路由用例据此断言跑完一个字节都没变；点开头的文件
按惯例不算工作区产物，别的用例的「没往工作区乱写」断言忽略它。

不含隐私检查器五类正则能命中的值：没有案号、没有手机号、没有身份证、没有统一社会信用代码、没有案件目录路径；
当事人与法院一律写「甲」「乙」「丙」。领域是真的「破产」（`skills/productivity/loo0ng-domain/assets/破产/`）：
ADR-0015「skill 层端到端用真实的破产领域目录」。
