# 种子「空目录」的状态

还不是案件工作区：没有 `图.json`、没有六格、没有工作区指针块。目录里只有一个 `收件箱/`（律师拷文件进来的唯一入口，起手前就可以有），装着六件待归档的合成文件，各对着归档的一个去向（ADR-0007）加一件既有成品：

| 文件 | 该去哪 | 为什么 |
| --- | --- | --- |
| `债务人移交物品清单.txt` | `材料/` | 本案事实来源原件 |
| `甲法院破产案件管理人工作提示.md` | `指南/` | 本案适用的官方要求文件（法院随决定书送达的报送要求） |
| `（格式）债权申报登记表.md` | `模板/官方/` | 官方发布的空白件，正文写明法院编发格式、待填 |
| `本所自用-接管物品交接单空表.md` | `模板/生成/` | 自己做的空白件，正文写明法院没发过统一格式 |
| `未命名.txt` | 留在收件箱 | 三格都无正向证据，是几行随手记的字 |
| `管理人承诺书（已交法院）.md` | `材料/`，并在起手清单上建议登记到节点「管理人承诺书及团队人员」 | 起手前就已写成、已报送法院的既有成品 |

领域是真的「破产」（`skills/in-progress/domain/assets/破产/`）：ADR-0015「skill 层端到端用真实的破产领域目录」。起手图取空图，所以 `指南/` 归档后跑的雏形是对着一张空图判重的：

- `管理人承诺书及团队人员` 与领域图里同名，判为「按领域图带入」，写入时沿用领域图的 id `n-chengnuoshu`，连带模块「接受指定与报备」。
- `联络人备案表` 领域图里没有，判为新，进指南里那个新模块「本院专项要求」（模块标题由模型从指南拟，用例只断言这个节点在图里）。
- 与图里、领域图里相似不同名的标题一个也没有，一句话拍板时不留待定。

供 skill "setup-case" 的用例「起手」：律师在这个目录里打一句起手，断言六格、`图.json`、两份视图、工作区指针块、归档去向、雏形写进图的节点、既有成品登记为已生成来源律师、没有任何节点被自动确认。

不含隐私检查器五类正则能命中的值：没有案号、没有手机号、没有身份证、没有统一社会信用代码、没有案件目录路径；当事人与法院一律写「甲」「乙」「丙」。

ADR-0015 的「空目录（起手三选一、机制 B）」加它留的那句「收件箱含既有成品的场景等起手清单实现票再加」，在 #31 合成这一个种子。
