# 种子「空图」的状态

**空图起手**的案件工作区（走真的起手 CLI，不给预设图）：目录形状齐全、指针块（「预设图：无（空图起手）」）与两份视图都在，`图.json` 的 `模块` 是空数组，一个节点都没有，没有任何条目、没有文书、`参考/模板/` 空（空图起手不拷模板）。

案件图自足（ADR-0023）：路由没有别处可看，当前节点没有、上一完成没有、前方为空，落在四问的问 4；「你在哪」该直说空图，下一句要么是律师自己定标题的 `doit 出一版 <标题>`，要么明说没有下一句，**不能**从出厂预设图里编出「管理人承诺书及团队人员」之类的节点来。

供 skill "ask-loo0ng" 的用例「空图起手」。

回放末尾把三份图文件的 sha256 写进工作区根的 `.基线.json`（`evals/共用/基线.py`），路由用例据此断言跑完一个字节都没变。

不含隐私检查器五类正则能命中的值。
