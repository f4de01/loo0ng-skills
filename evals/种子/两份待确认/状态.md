# 种子「两份待确认」的状态

按真实的破产领域目录**整份起手**（12 模块 72 节点全部在图里），再摆出三个节点：

| 节点 | 状态 | 条目 |
| --- | --- | --- |
| 管理人承诺书及团队人员 | 已确认 | 一条生成（来源 agent）、一条确认（来源 律师，原话「承诺书这份就这样，可以了」） |
| 管理人印章备案报告 | 已生成 | 一条生成，没拍板 |
| 管理人银行账户备案报告 | 已生成 | 一条生成，没拍板 |
| 其余 69 个 | 未生成 | 无 |

两份同时待确认，图里的顺序是印章备案在前、银行账户备案在后。印章备案在领域图上带时限句
（自收到《刻制管理人公章函》之日起 3 日内……），银行账户备案没有。
案件图里最早的未生成节点是同一模块的「管理人工作计划」，它没有时限；
排在它后面的「内部管理制度备案报告」「刻制管理人公章的申请」倒是有时限：
路由若因时限改序就会挑错，这一条正是「不因时限改序」的判据。

供 skill "ask-loo0ng" 的用例「两份同时待确认」。

两份视图是最后一次写图后重算的。回放末尾把 `图.json`、`图视图.md`、`图视图.json` 三份的 sha256 写进
工作区根的 `.基线.json`（`evals/共用/基线.py`），路由用例据此断言跑完一个字节都没变；点开头的文件
按惯例不算工作区产物，别的用例的「没往工作区乱写」断言忽略它。

不含隐私检查器五类正则能命中的值：没有案号、没有手机号、没有身份证、没有统一社会信用代码、没有案件目录路径；
当事人与法院一律写「甲」「乙」「丙」。领域是真的「破产」（`skills/in-progress/domain/assets/破产/`）：
ADR-0015「skill 层端到端用真实的破产领域目录」。
