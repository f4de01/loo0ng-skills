# 种子「图引擎」的状态

按合成小领域「菜园」（`evals/领域/菜园/`）整份起手，走的是真的起手 CLI（skill "setup-case"）：4 模块 9 节点全部在图里、都未生成、没有条目；`图视图.json` 的前方为空，3 个节点在视图里带时限句。工作区根有 `AGENTS.md`（领域名与领域目录绝对路径、图与视图文件名、入口名、只读声明）与一行 `@AGENTS.md` 的 `CLAUDE.md`，六格（`收件箱/`、`材料/律师陈述/`、`指南/`、`模板/官方/`、`模板/生成/`、`文书/`）都在，都是空的：合成小领域没有 `模板/`，所以 `模板/官方/` 也空。

没有收件箱文件、没有文书、没有律师陈述。供 skill "graph" 的用例：律师一句话改构成，断言 `图.json` 与两份视图。
