# 种子「图引擎」的状态

按合成小领域「菜园」整份起手：它是一份**个人预设图**，由 `evals/共用/回放助手.py` 在家里（`LOO0NG_HOME` 下的 `预设图/菜园/`）用引擎 `--kind preset` 现造（4 模块 9 节点、两个带时限、两个挂空白模板，与 `tests/共用/工作区.py` 同一张表），再走真的起手 CLI `init --preset 菜园 --owner 个人`：目录形状、图与两份视图、两件空白模板（`参考/模板/施肥记录.docx`、`播种登记.docx`，最小的合成 docx）、指针块（「预设图：菜园（个人）」）都由它落。9 个节点都未生成、没有条目；前方是全部九个。

没设 `LOO0NG_HOME` 时家落在工作区里的 `.预设图家/`（点开头），永远不碰律师真的 `~/.loo0ng/`。

没有待归档文件、没有文书。供 skill "graph" 的用例「改标题」与 skill "doit" 的用例「换节点被拒」（「松土」与「除草」都没有空白模板）；种子「待归档」「模板」「指南」在它之上各加一样。工作区里没有任何案件内容，也没有破产语义（ADR-0015「领域两用」）。
