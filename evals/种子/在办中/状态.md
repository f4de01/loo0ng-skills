# 种子「在办中」的状态

一个已经办了几步的案件工作区：按出厂预设图「破产」（`skills/engineering/domain/assets/预设图/破产/`）起手，12 模块 72 节点整份在图里，`参考/模板/` 下 19 件官方模板原件由起手整份拷入，工作区根有只记名与归属的指针块（`AGENTS.md` 里「预设图：破产（出厂）」，一条路径都不记）与一行 `@AGENTS.md` 的 `CLAUDE.md`，都由真的起手 CLI 落。领域取真的而不是合成小领域「菜园」：ADR-0015「skill 层端到端用真实的领域」，出件的用例也只有在这里才有真模板可用。

| 节点 | 状态 | 条目 | 文书 |
| --- | --- | --- | --- |
| 管理人承诺书及团队人员 | 已确认 | 一条生成（agent）、一条确认（律师，原话「承诺书这份就这样，可以了」） | 合成 docx，没有黄（高亮 已清） |
| 管理人印章备案报告 | 已生成 | 一条生成（agent），没拍板 | 合成 docx，一处黄（高亮 未清） |
| 其余 70 个 | 未生成 | 无 | 无 |

- 文书按 `文书/<模块>/<节点>/<节点>.docx` 加同名 `-审查报告.md`，都是合成的几句话，审查报告四段固定形状。
- `材料/债务人移交物品清单.txt` 由跑器拷进 `待归档/`、回放经归档 CLI 搬进 `材料/`，`归档索引.md` 有它那一行。
- `待归档/甲法院破产案件管理人工作提示.md`（从种子「空目录」借来）**没归**：出一版开场那次「清待归档不问」有东西可清。
- `材料/律师说过的.md` 四行：受理裁定日期与当事人、管理人与负责人、收到决定书的日期、先办印章再办账户。账户信息一句没有，出一版银行账户备案时那几格必留黄。
- 两份视图是最后一次写图后重算的；前方第一个是「管理人工作计划」。回放末尾把三份图文件的 sha256 写进 `.基线.json`（`evals/共用/基线.py`），路由用例据此断言只读；点开头的文件不算工作区产物。

供 skill "doit" 的用例「出一版留黄」「记一句」「拍板」「模块不适用」与 skill "ask-loo0ng" 的用例「问了图里没有的事」；种子「兜底」「拒改」在它之上再摆一层。

不含隐私检查器五类正则能命中的值：当事人与法院一律写「甲」「乙」「丙」。
