# 种子「在办中」的状态

一个已经办了几步的案件工作区：按真实的破产领域目录（`skills/productivity/loo0ng-domain/assets/破产/`）整份起手，12 模块 72 节点全部在图里，六格齐全，`模板/官方/` 下 19 件官方模板原件由起手按图上挂到的那几件拷进来，工作区根有 `AGENTS.md` 与一行 `@AGENTS.md` 的 `CLAUDE.md`（都由真的起手 CLI 落，不是回放自己写的）。领域取真的而不是合成小领域「菜园」：ADR-0015「skill 层端到端用真实的破产领域目录」，出件的用例也只有在这里才有真模板可用。

| 节点 | 状态 | 条目 |
| --- | --- | --- |
| 管理人承诺书及团队人员 | 已确认 | 一条生成（来源 agent）、一条确认（来源 律师，原话「承诺书这份就这样，可以了」） |
| 管理人印章备案报告 | 已生成 | 一条生成（来源 agent），没拍板 |
| 其余 70 个 | 未生成 | 无 |

- 这两个节点各有一个 `文书/<节点标题>/` 目录，里面一份合成的 `.md` 与同名 `-审查报告.md`；两份报告都是五段固定的形状，印章那份的时限段写着基准日缺失。
- `材料/债务人移交物品清单.txt` 是合成的移交清单，两份审查报告的生成依据指着它。
- `材料/律师陈述/` 下两条：一条直接陈述（收到指定决定书的日期，关联节点「管理人承诺书及团队人员」）、一条裁定（先办印章再办账户）。文件名里的时刻是回放当时的时刻，每次回放都不同。
- `收件箱/`、`指南/`、`模板/生成/` 都在，都是空的。
- 两份视图是最后一次写图后重算的：模块与节点的来源都是「领域图」，24 个节点带时限句，前方为空。
- 回放末尾把这三份文件的 sha256 写进工作区根的 `.基线.json`（`evals/共用/基线.py`），供路由用例断言只读；点开头的文件不算工作区产物，别的用例的「没往工作区乱写」断言本来就忽略它。

供 skill "loo0ng-doit"（#32）与 skill "ask-loo0ng"（#33）的用例：拍板一个已生成未确认的节点、在一个已确认的节点上重出、问「我现在在哪、下一步做什么」。#31 只交种子本身，用例随那两票来。

不含隐私检查器五类正则能命中的值：没有案号、没有手机号、没有身份证、没有统一社会信用代码、没有案件目录路径；当事人与法院一律写「甲」「乙」「丙」。
