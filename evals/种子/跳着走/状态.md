# 种子「跳着走」的状态

按出厂预设图「破产」整份起手，再摆出律师跳着走的样子：

| 模块 | 节点 | 状态 |
| --- | --- | --- |
| 接受指定与报备 | 管理人承诺书及团队人员 | 已确认 |
| 债权通知与审查 | 债权表 | 已确认（确认最晚，上一完成就是它） |

案件图自足（ADR-0023）：前方只从图自己的条目算，按图序从第一个模块起，第一个没生成的是「管理人工作计划」。路由照前方给、不自己重排，不因为律师最近动的是债权那一块就跳到「债权申报与审核流程规则」。

供 skill "ask-loo0ng" 的用例「跳着走」。

两份视图是最后一次写图后重算的。回放末尾把三份图文件的 sha256 写进工作区根的 `.基线.json`（`evals/共用/基线.py`），路由用例据此断言跑完一个字节都没变。

不含隐私检查器五类正则能命中的值：当事人与法院一律写「甲」「乙」「丙」。
