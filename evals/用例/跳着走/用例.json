{
  "说明": "路由 skill \"ask-loo0ng\"：律师跳过前面几块先办了后面一块。Claude Code 侧拼成 /ask-loo0ng <提示词>，Codex 侧用替身提示词，测的是正文不是触发。种子「跳着走」空图起手，模块「接受指定与报备」的承诺书与模块「债权通知与审查」的债权表都已确认，两块各只有这一个节点进了图。断言命中问 3、下一句是同一块里领域图顺序最早的「债权申报与审核流程规则」、最后一段里不出现回头补空才轮到的「管理人工作计划」（问 3 排在问 4 前面的取舍，见 references/下一任务.md）、上一完成是确认最晚的债权表，再加共用的六条与只读基线。测不出的：一次调用里看不出模型是不是真的去读了 doit 的 SKILL.md。 实测 Claude Code 侧 6 回合、Codex 侧 7 回合，上限 30 留足余量；路由只读，允许工具只给 Read / Glob / Grep（正文写着「不写任何文件」，不给 Bash），Codex 侧 workspace-write 沙箱够用（不跑 python、只读工作区与领域目录）。 已知抖动：Codex 偶尔把回答拆成两条消息，跑器按文档只取最后一条（`docs/agents/skills.md`「`回复正则` 对最后一条回复做 `re.search`」），撞上时整组段落断言一起红，重跑即过（`--runs`）；正文已写明「五段一次给全，写在同一条回复里」。",
  "种子": "跳着走",
  "skill": "ask-loo0ng",
  "回复正则": "命中问\\s*3",
  "回合上限": 30,
  "超时秒": 300,
  "允许工具": [
    "Read",
    "Glob",
    "Grep"
  ],
  "Codex沙箱": "workspace-write"
}
