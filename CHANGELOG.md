# loo0ng-skills

## 0.3.0

### Minor Changes

- [`330a96a`](https://github.com/f4de01/loo0ng-skills/commit/330a96aaa66188f12e2c846281de045e61606695) - 三处回归上游 mattpocock/skills 的做法，仓库迁到 `f4de01/loo0ng-skills`（ADR-0021）：

  - **发布链**改为 changesets/action：push 到 main 自动开 "chore: version skills" PR，合并即打 tag。`release.py`、`pack-offline.py` 与干跑流程删除，Release 不再附离线兜底包。
  - **不再发 Codex 原生插件**：`.codex-plugin/` 删除。Codex 用户改用 `npx skills@latest add f4de01/loo0ng-skills`；skill 在 Codex 里显示为裸名。
  - **`agents/openai.yaml` 改为手写**：frontmatter 去掉 `metadata` 块，`gen-openai-yaml.py` 删除。skill 名、入口、正文都没有变。

  **Breaking:** 安装源换了仓库。请卸掉从 `lawyer-workbench-v3` 装的那份，按 README 从新仓库重装。

### Patch Changes

- [`2cc2283`](https://github.com/f4de01/loo0ng-skills/commit/2cc2283574317d8b93553cdb06b283dde11a6cf9) - 仓库布局改成严格照上游 mattpocock/skills 分桶（无票，ADR-0009 的 2026-09-13 附注）：七件 skill 从 `skills/<name>/` 搬进 `skills/productivity/<name>/`，另建 engineering、misc、in-progress、deprecated 四个空桶各带 `README.md`，`skills/README.md` 删除；根 `README.md` 与桶 `README.md` 的条目把名字链接到 `SKILL.md`；每件新增一页 `docs/productivity/<name>.md`。`.claude-plugin/plugin.json` 的七条路径跟着改，`link-skills.ps1` 照上游跳过 `deprecated/` 与 `misc/`，`privacy-check.py` 守的领域图前缀改为新路径。skill 名、入口、装到律师机上的形状都没变。

## 0.2.0

### Minor Changes

- a64ddc4: 开发侧那两条路搬去写活图（#102，ADR-0020）：`sketch.py apply`（对通用指引手册跑雏形）与 `sketch.py from-case`（从案件图算批量候选）的终点从包内出厂种子改成开发者本机的**活图**，路径由 `sketch.py home --name <领域名>` 回显。#99 已经把引擎那条无条件拒落下去了，这两条路原先写着种子，会直接撞上它。

  **零代码改动**（版号仍取 minor：变的是 skill 正文里那条路怎么走，而正文就是随包发的东西）：两条命令的形状一个字没变，换的是 `--graph` 与 `--domain` 指哪儿，正文里那几条命令示例跟着改。ADR-0020 当时就核过"`from-case` 只认 `--domain` 给的那个目录，指活图零改动"，这一版把它钉成断言。

  `references/回流.md` 重画：两条路的终点统一成活图，差别只剩**粒度与时点**（律师侧一次一个节点、在确认那一刻；开发侧一批、没有时点，补历史与定制时做）。开发侧那节不再有"开分支"与"发布"两步：这条路不碰仓库里任何文件。第二双眼那一行跟着改：它守的那次写挪到了**入库**（活图那份 `领域图.json` 进仓库成为出厂种子，一次 git 提交，走分支 + PR + 隐私钩子），不在回流这一刻（ADR-0012 的 2026-09-10 附注）。末尾新增「入库」一节，把那一次写的把关写全；`intake` 本身是另一票，落地之前不写它的步骤。

  **补历史那条路写明了**：律师（或开发者）把工作区 `AGENTS.md` 里「领域目录」那一行从种子换成活图之后，这个案子里此前已确认的节点**不会自动补上**，#91 定的是"不写、不再问第二次"。补它就走开发侧那条路、`--domain` 指活图；它算的是"案件图有、活图没有"的减法，已经逐节点回流进去的不会再提一遍。这一句只能落在 `references/回流.md`：`loo0ng-doit` 正文不许提 `from-case`（`tests/loo0ng-domain/test_isolation.py` 守着），`loo0ng-setup-case` 正文不许提回流（同一份测试守着）。

  `loo0ng-domain` 正文三处跟着改：雏形三种来源那张表里两行开发会话的"对着哪张图"与"产出"都从种子改成活图，表下补一句这两条路的 `--graph` 指活图、路径先跑 `home` 取（雏形四步对通用指引手册那条路原先没说这个）；「领域图：谁在写哪一份」那一节把活图挪到前面写成"两侧都写它"，种子那一条改成"谁都不许在会话里写，改由入库更新"。`README.md` 同改。

  守住它的是 `tests/loo0ng-domain/test_sketch.py` 新增的三条断言：跑完一次 `apply`，活图多出那几条、包内种子一个字节不变；同一份雏形冲着种子跑被引擎拒（"字节不变"才不是因为没人写它）；`from-case --domain <活图>` 走完整条路、保留原 id、整条路不碰种子。种子一律用临时假包（目录名摆成 `<…>/loo0ng-domain/assets/<领域名>`），与 `tests/loo0ng-graph/test_package_guard.py` 同一个理由：拿真包测就是拿开发者的工作树赌一次规则。

  `loo0ng-setup-case` 的 `setup.py` 行为一个字没动（给了包内种子仍**只报不拒**，那是起手那一头，本票边界之外），改的只是那句已经成假话的理由：原文写"开发侧的种子回放与**回流**本来就直接对着种子跑"，回流现在不对着种子跑了；新的说法是"起手是读，写那一刻另有引擎无条件拒着"。种子「回流」的 `状态.md` 同改一处："下一次**回流**让领域图长大"改成"下一次**入库**让出厂种子长大"，并写明工作区根上那份可写副本站位的是开发者本机的活图。

  eval 用例「回流」的目标路径没动（工作区根上那份领域图可写副本），它与活图形状相同：都是包外的一份领域图，不是随包分发的种子；`用例.json` 的"局限"改写成实话，并点明测不出的是"先跑 `home` 取路径"那一步。`references/回流.md` 第 1 步也写明：开发者那句话里直接给了路径时用他给的那个。

- 1adf372: 开发者造或维护一个领域，从头到尾一条路走通（#103，ADR-0020）：`sketch.py home --empty` 起头，`sketch.py intake` 入库收尾，中间那段（办一遍、逐节点回流进活图）走的就是律师那条路，没有另做。

  **`home --empty`**：`home` 原先在「活图位置上没有这个领域、包里也没有种子」时拒，而开发者要造一份全新领域的种子，起点恰恰是这里。加 `--empty` 之后它在活图位置建目录、经引擎 `init --kind domain --name <领域名>` 起一份空领域图，回显仍是那一行绝对路径（`活图：<路径>`），下游一个字不用改。包里已经有出厂种子时它拒（不带 `--empty` 再跑一次就是从种子整份拷一份），不给种子盖一份空的。写图仍只经引擎子进程；与拷种子同一个形状，先落临时名再改名，引擎拒了就一格不留。两侧同一条路的意思就是开发侧也享受 ADR-0019 那条低入场费（新领域从空图起手），卡在「活图建不出来」那就不是同一条路。

  **`intake`**：把开发者活图那份 `领域图.json` 送进本仓库成为出厂种子。**只拿这一份文件**，不拿整个领域目录（官方模板原件与指引手册原文是原件，走普通的 git 添加；整份拷会把活图目录下的任何东西卷进仓库，正是硬边界 1 最不想要的形状，`AGENTS.md` 结构不变量 5 因此不受影响）。拷之前先经引擎 `validate` 校验（格式合法、无条目，那是读不是写），**再把这次比种子多了什么、改了什么分栏回显**：新增、改名、改属性、改归属、种子里有而活图里没有，各占一栏，头一行数出「多了几个模块几个节点、改了几个标题」。这行分栏是本票的重点：两侧同一条路带来一个原来没有的风险，开发者的活图里混着他试办案子回流进去的东西，入库从「逐条精挑」变成「整份送进去」，第二双眼要审的 diff 一下子变大且没有摘要，这几行是隐私钩子与第二双眼之前的第一道人眼过滤。种子根不在一个 git 工作树里（装好的那份 skill 包）就拒，律师那一侧永不入库；活图里写的领域名与 `--name` 对不上也拒；与种子逐字相同就一个字不拷。子命令不叫 `publish`：入库不是 skill 包的版本发布（ADR-0009「维护即发布」说的是后者），一次包发布可以带零次或多次入库；入库须经第二双眼（硬边界 2），审的就是那段 diff。它是一次拷贝、不经图引擎，所以不受 #99 落下的那条「包内种子谁都不许写」约束，那条拒管的是会话里的每一次写图，照旧立着，入库没有绕过它，只是走的另一条路：一次要人审的 git 提交。这三句正文里写着，免得日后有人以为它绕过了守门。

  正文三处跟着改：`loo0ng-domain` 加「入库」一节、活图那节加 `home --empty` 一行、种子那条的「改由入库更新」指向新那一节；`references/回流.md` 开发侧第 1 步加 `--empty`，末尾那节把「还没落地，别去调它」换成真的命令与它的规矩；`CONTEXT.md`「入库」词条补上命令名与「拷之前校验并分栏回显」。断言在 `tests/loo0ng-domain/test_home.py`（新领域从空图起手六条：建目录并起空领域图、回显第一行仍是绝对路径、起出来的图引擎认、包里有种子就拒且不建目录、已有活图就一个字不动、引擎拒了不留半个活图）与新增的 `tests/loo0ng-domain/test_intake.py`（15 条，含本票要的那一条：新增与改名各占一栏、不混在一起，改前改后都写出来）。

  **同时修一处正文的洞**（律师 0.1.0 现场报的）：起手时律师只说了「空图，自己一步步长」、没说领域，agent 拿本机上唯一那个「破产」种子当默认起了一张破产领域的空图，律师说「我要做新领域的空图」才纠正。`loo0ng-setup-case` 正文原先把起手图三选一写成「只问这一件」，领域名默认从环境里来，这就是那个洞。改成：要定的是两件（哪一种起手图、这案子是哪个领域），一次问齐；**领域名只能由律师给，不许从本机上有什么推出来**（种子有哪些是安装的事，案子是哪个领域是律师的事）；律师答的是一个此前没有过的领域名，那不是笔误，是「新领域从空图起手」那一行。起手图三选一那张表因此拆成四行，空图分成「这个领域有领域目录」与「全新领域」两种，后者 `--domain-name` 与 `--domain` 都不给、指针块「领域目录」记「无」；收尾第一段那行起手图连领域名一起念。新增 eval 用例「空图没给领域」守它：律师那句话里空图说清了、领域没说，断言图与两份视图没落盘、一格没建、收件箱原封不动、回复里问了领域。Claude Code 侧实测：stash 掉正文这一处改动跑同一条用例就红，回的正是「领域名你没说，我按 `破产` 起的」；改后两侧都绿。

- 02346dc: 领域目录搬出 skill 包（#90，ADR-0019）：`~/.loo0ng/领域/<领域名>/` 是**活图**，包内 `skills/loo0ng-domain/assets/<领域名>/` 降为**出厂种子**。挡的是这条路线唯一的不可逆事故：领域图原先随包分发，律师再跑一次 `npx skills add` 升级就被整个覆盖，累计半年的图一次抹掉。`loo0ng-domain` 的雏形 CLI 多一条 `home --name <领域名>`：活图不在就从种子整份拷一份（破产那份 22 件约 540 KB，实测 0.2 秒，起手上感觉不到），已经在就一个字不动、只回显路径，回显第一行是 `活图：<绝对路径>`；活图与种子都没有的新领域它拒，那个领域从空图起手。拷贝先落临时名再改名，拷到一半断了不会留下半份被下一次当成活图。默认位置在用户主目录而不是案件根旁边：领域目录是跨案件的，不该跟着某个案件目录搬家；环境变量 `LOO0NG_HOME` 换「家」那一层，脚本层单测与 eval 跑器用它，律师那台机上不设。

  `loo0ng-setup-case` 起手第 1 步先跑 `home` 取路径，再 `init --domain <活图>`；工作区 `AGENTS.md` 里「领域目录」那一行从此指活图，之后每一件 skill 都从那一行读，机制没变、没有新增确认点。`--domain` 给成包内种子时起手照做并报一句（开发侧的种子回放与回流本来就直接对着种子跑，所以只报不拒）。起手三选一不变，「整份领域图」的所指改成活图，开发者定制分发的图仍走 `--from`，两者并存不自动合并。**0.1.0 起手的工作区本版不迁移**：它们的 `AGENTS.md` 仍指着种子、仍读得到，要改用活图就手改那一行，处置写进了 `loo0ng-setup-case` 正文。`references/回流.md` 写明它描述的是开发侧那条路、写的是种子。

  这一版只把领域目录搬出包，**ADR-0019 点名的另外两件 skill 不在本次**：`loo0ng-doit` 的律师侧逐节点回流、`ask-loo0ng` 定位时从活图取图，各有自己的票；`from-case` 与 `references/回流.md` 那条开发侧的路一个字没动，只写明了它写的是种子。

  守住这条保证的是四条断言：**改动种子模拟一次包升级之后活图逐字不变**（本票存在的理由）、已有活图不覆盖、首次起手真的拷出三样、工作区 `AGENTS.md` 记的是活图不是种子。两侧 eval 照跑；跑器给每次运行一个临时的活图家，不往开发机的主目录里拷东西。

- a9fbb64: 逐节点回流（#91，ADR-0019 的主体）：律师确认一份文书之后，若领域图里没有这个节点，agent 问一次它该归哪儿，律师一句话之后才经引擎写进本机**活图**。这是律师累计自己那张领域图的唯一机制，也是泛化那条路的发动机：新领域从空图起手，图靠办案长出来。

  - **时点只有一个**：确认那一刻。ADR-0008 的确认点不变（读审查报告后一句话），这一问接在它之后，不塞进生成中间；不适用不问。
  - **只对领域图里没有的节点问。** 判定是按 id 的反向减法，引擎早就算好了：`图视图.md` 里那个节点的「来源」是「领域图」就一个字不问，是「案件」才问。问的次数靠这一条随累计衰减到零；写死成每次都问就是把 2.0 的反复确认搬回来。
  - **两个默认值**：模块继承它在案件图里所在的那个，标题取去掉当事人、案号、金额、日期之后仍成立的写法（与 `from-case` 那套改写同一个口径，不写第二套）。**不给时限句**，律师侧新增的节点时限缺省，开发者维护出厂种子时补。
  - **未拍板不写**，与雏形机制同一条规矩；写入仍只经 `loo0ng-graph` 的引擎（`--kind domain` 指着活图那份 `领域图.json`），id 用案件图里那个，否则本案的前方立刻多出一个与自己重复的节点。律师改模块、改标题、跨模块移动、新建或改名模块，用的都是引擎现成的能力：**不新增 CLI、不新增 skill**。
  - **这条路没有第二双眼**，理由写进了两处正文：活图在 skill 包与本仓库之外，硬边界 1 与隐私钩子都管不到它；去案件化在这条路上是可用性问题（别让 A 案当事人的名字出现在 B 案的图里），不是隐私门禁。进仓库的出厂种子那条仍要第二双眼。

  跟着改的还有一处：`loo0ng-graph` 的正文原先写「领域图只在开发会话里改、办案会话对它只读」，与「图的唯一写入口包括活图」正面打架，模型读到它会拒写，现在改成两份分开说（种子只读、活图在办案会话里写得，只有确认之后那一条路）。

  已知的一个洞，另票堵（#99）：0.1.0 起手的老工作区，指针块那一行还指着 skill 包内的出厂种子（#90 裁定本版不迁移），在那样的工作区上走这条路写的就是本仓库里的文件，破硬边界 2。本票不判它。

  正文两处：`loo0ng-domain` 是回流机制的主人（`references/回流.md` 改成两条路一张对照表，律师侧那条的规矩写在前面），`loo0ng-doit` 是回合的驱动者（那一问挂在它的收尾，缺失判定与写入命令写在它的正文）。`from-case` 与开发侧那条路一个字没动，它降为补历史与定制分发的入口。

  验收：新种子「逐节点回流」（合成小领域「菜园」上的案件工作区加一份活图，一个领域图里有、一个没有的待确认节点）加四条 eval，两侧全绿。领域图里没有的（活图多出那一个，模块继承默认、id 与案件图一致、条目为空、没有时限属性）、领域图里已经有的（提示词预先授权直接写，活图仍逐字不变，测的是缺失判定不是问话的礼貌）、律师改了默认归属的（按他说的模块与标题写）、律师还没拍板的（**活图逐文件 sha256 一个字节不变**）。脚本层另加种子的九条与「回流两条路」的会话边界四条。

- c4bb146: 包内出厂种子谁都不许写（#99，ADR-0020）：`graph.py` 对 `--kind domain` 落在 skill 包内的写**无条件拒**，没有放行口。挡的是 ADR-0019 与 #90 合起来留的一个洞：0.1.0 起手的工作区指针块指着包内种子，#91 把逐节点回流的路开出来之后，在这样的工作区里确认一次就会把律师累计的节点写进随包分发的那份文件，下一次 skill 包升级整个换掉；而律师那台机器上没有 git、没有隐私钩子，丢失是静默的。

  裁定比堵洞高一层：**领域图的构建，开发侧与律师侧走同一条路**。开发者要定制或维护一个领域图，也是办一遍、逐节点回流进自己那份活图。所以引擎里不需要"开发侧放行"那个分支，原本设想的 `--seed` 旗标没有采用。出厂种子改由**入库**更新（活图那份 `领域图.json` 进仓库，一次 git 提交，经隐私钩子与第二双眼），它与 skill 包的版本发布是两回事。

  **只拦写，不拦读**：`validate`、`views`，以及案件图拿种子当领域图来源（`--domain` 指着它起手、算前方、惰性带入）一条不碎，那是 eval 的回放助手与所有 0.1.0 工作区每天在走的路。这条规则**只管 `--kind domain`**：案件图落进包内是另一条硬边界的病，且该在起手那一刻拦。判据两条取或，与 `loo0ng-setup-case` 的 `setup.py` 同源（按脚本位置算出的兄弟 `assets/`；目录名摆成 `<…>/loo0ng-domain/assets/<领域名>`），两处各留一行互指注释；不加"祖先里有 `.git`"这条，它会误伤把活图放进 git 备份的律师。

  拒绝消息把出路一次说全：活图在哪、路径怎么取、这个工作区 `AGENTS.md` 里「领域目录」那一行怎么换。`loo0ng-doit` 正文补一条：被拒时**提案照样回显**，照实转告，**那一行由律师自己改，别替他改**。`loo0ng-graph` 正文里"出厂种子只在开发会话里改"改掉，开发会话也不行。起手那一头一个字没动（`setup.py` 仍只报不拒）：洞在写入那一刻已经堵死。

  `CONTEXT.md` 三处：「领域图」与「回流」的终点统一成活图，「第二双眼」守的那次写从回流挪到入库，新增「入库」词条。

  守住它的是脚本层 15 条断言加一条 eval 用例。**两侧都不碰仓库里真的 `assets/`**：拒的行为拿临时假包测（判据 ② 认得它的形状），真包只留一条只读断言钉住"它确实落在判据 ① 的射程里"，两条合起来才等于"包内种子字节不变"。拿真包跑就是拿开发者的工作树赌一次规则。eval 用例「指针指着种子被拒」测模型那一侧：确认照样落在案件图上（这条守门不该把办案挡住）、种子逐字不变、提案照样回显、回复里说清活图与那一行、没替律师改那一行、没在工作区里另造一份领域图。

  ADR-0020 点名的另外两件不在本次，各有自己的票：`apply` 与 `from-case` 改成写活图（连同 `references/回流.md` 重画），以及 `sketch.py intake` 与 `home --empty`。落地顺序是先堵洞再搬家：中间那段时间开发侧那几条路会撞到拒绝，撞到的人从 ADR-0020 知道该往哪走。

### Patch Changes

- 1664b27: Codex 侧「出雏形」列为已知抖动，合入门槛给它一件豁免（#68）。这件红两种漏法（回显漏空白模板名、漏时限）在 `fix/65-codex-三件红` 上 4 次红 2 次，stash 掉全部改动回到 main（`8e29df3`）跑同样 3 次仍红 2 次，与任何一次改动都无关；同一份 `loo0ng-domain` 正文在 Claude Code 侧稳定给全。律师 2026-09-08 裁定全量 19/20 可以通过，于是 ADR-0015 加一条同日附注、`docs/agents/skills.md` 的门槛段跟一句：合入门槛仍是「两侧 eval 全绿」，例外只有 `出雏形` 这一个用例、只在 Codex 侧、且只在其余 19 件全绿时成立，别的用例红仍是阻塞，PR 里要点名这件红并写明与本票无关。这条不改 ADR-0015 正文那句「抖动是正文问题，不用多数决盖住」：那句禁的是复跑取多数把红盖成绿，这里是把一件已知红原样记在门槛旁边、不复跑也不改判。`loo0ng-domain` 的回显段下次被改动时这条一并复核。正文与用例一个字没改。
- f19b0cd: `loo0ng-doit` 第 3 步补一句「什么样的回复算没走这一步」。Codex 侧 `换节点被拒` 在本轮干净全量里红了一次（今天 4 次红 1 次，复跑 3 次全绿，加上 #65 那轮的 12 次，频率约 1/16）：模型把律师一句话里的两个节点并成一句一起答了「都缺材料、指南及空白模板」，第二个节点既没被拒、也没拿到开新对话那两行。正文原本已经写了要做什么（#65 把换节点检查提成死顺序第 3 步，并写明 A 落进第 5 步「三样全无」也不改变 B 被拒），缺的是写明什么样的回复算没做，于是模型一旦把两个节点当成一件事就整段绕过去。照 #65 对 `ask-loo0ng` 五段标题用过的写法补一句：两个节点并进同一句算没走这一步，只答了 A、B 一个字没提也算没走，不是格式略有出入，内容答得再对也一样；发出去之前数一遍，律师这一句里点了几个节点回复里就得有几段各自的结果，被拒的那个身上原样带着那两行。用例与断言一个字没改。这一件的频率低到 eval 回路验不动它：1/16 的红，再跑一轮全绿的全量不构成「修好了」的证据，所以本次不为它重跑两侧全量（律师 2026-09-08 定），证据只到「正文补了一处可指的缺口」为止。
- 1664b27: `loo0ng-filing` 正文写死「一条」的边界（#69）。Claude Code 侧 eval 用例「记一句」偶发红：一句话里含两个事实（「去年种的是番茄」加「今年别再种茄科」）被落成两个陈述文件，`check_文件名格式`、`check_头部六字段`、`check_原话逐字` 三条一起卡在「应恰有一个文件」。原因在正文：落档规则写的是「一条一文件、原话逐字」，但从没写过「一条」按什么数，模型于是有时按句里的事实件数拆。现在在「陈述」一节的落档规则之前单起一段写硬：一条等于律师说出的那一句，不是那句话里的事实件数；含几件事都不改变条数，仍是一次调用、一个文件，多个值的出路是 `--item` 列进整理表而不是拆成几个文件；真要成两条得律师自己分两次说；落档前数一遍，同一句话落出第二个文件就是落错了。理由照 ADR-0013 的原话逐字：拆开之后没有哪一份的原话是他说的那一句。断言与脚本一个字没改（`_only_file` 的口径本就是对的），照 ADR-0015「抖动是正文问题，不用多数决盖住」。顺带补一处 `.changeset/close-trigger-workspace.md` 里漏网的破折号（U+2014），机械检查恢复无输出。
- cb5f327: 门禁加一项不通过「模板表缺失」，封住 #107 那条抖动的因。

  `不通过收尾` 两侧抖动的因不在 skill 正文，在钉子：种子 `越界` 把行高钉在模板第一张表的第一行，而转换器只在稿子里有管道表时才把模板那张表落进成品。稿子里写不写那张表是模型的自由，于是有两条真实走法把这件跑红（现场都留在 `%TEMP%/loo0ng-to-docx/`）：一条是 v1 就把账户三项写成正文段落、第一次门禁就通过；另一条是 v1 照模板写了表、门禁点名「行高超阈值：第 1 张表第 1 行」，模型按正文「按不通过项改稿重转重检至多 2 次」老老实实改稿，改的正是那张被点名的表，把它改写成段落之后门禁转通过、件落了盘、图上多了一条生成条目。四条断言一起红，而红的原因是钉子落空，不是模型违规，两者在断言上完全同形。票里那条「多半没真跑门禁」的线索是误读：门禁跑了，而且合法地给了通过。

  两件事跟着改：

  **门禁多一条判据**：给了 `--template` 时，模板有表而成品一张表都没有即不通过「模板表缺失」。它本来就该有：模型为了过几何判据把模板规定的那张表整个删掉，交出去的是一份缺了正文结构的件（一份没有账户表的账户备案报告）。没给 `--template` 时不查，与「页脚 PAGE 域丢失」「模板原文残留」同一个口径，那条披露项跟着改口。`tests/loo0ng-to-docx/test_gate.py` 新增一条：同一份 fixture 只把那张管道表改写成段落，有表必过、无表必拦、不给模板不算错，两条跑道都跑。

  **两份 skill 正文各加一句边界**：改稿只改字句，不删模板规定的正文结构（表、落款、附注）；不通过项指着模板带来的几何（行高、表宽）时，改稿本来就改不动它，直接判生成失败。这是事故的教训，按 `AGENTS.md`「本文件写什么」写进它所属 skill 的正文，不是 `AGENTS.md`。

  种子 `越界` 与用例 `不通过收尾` 的断言一个字没改（票的「边界」）：只在 `状态.md` 与断言的 docstring 里记下「模型无论怎么改稿都改不动它」这个前提已被实测推翻，以及封它的是门禁不是钉子。姊妹用例 `需人眼落盘` 早有 `check_成品里有模板那张表` 分辨钉子落空，这件没有，现在由门禁在更上游拦住，两件都不再需要那条断言来分辨。

- 2548af1: 用例「回流」真跑活图（#105，ADR-0020）：#102 把开发侧那条路的终点改成了活图、第 1 步改成 `sketch.py home --name <领域名>` 取路径，用例没跟上。种子把包内种子拷成工作区根上的 `领域图.json`，提示词直接点名「回流进 ./领域图.json」，于是第 1 步测不出，提示词与正文还打架（模型照 `references/回流.md` 会去跑 `home`，提示词却给了另一个显式目标），是一处抖动来源。`skills/` 下一个字没改，所以是 patch。

  - **种子 `evals/种子/回流/回放.py` 改成跑 `home`**：不再往工作区根拷领域图，改为 `sketch.py home --name 破产` 建活图（整份 22 件、约 540 KB，多零点几秒）。取路径这一步直接用 `evals/共用/回放助手.py` 里现成的 `用活图()`，与七个路由种子同一份写法，不另写一份；包内出厂种子的路径也取它的 `出厂种子` 常量。活图家取 `LOO0NG_HOME`，跑器每次运行另建一个临时的交给两侧 harness；没设这个变量时（`--materialize`、脚本层单测）落在工作区里的 `.活图家/`，永远不碰开发者真的 `~/.loo0ng/`。
  - **基线改成 `.活图基线.json`**（照种子「逐节点回流」那套）：活图的绝对路径、逐文件 sha256、模块数、节点数，外加案件图的 sha256。断言从基线里读，不硬写路径也不硬写 72。读法（`基线`、`活图目录`、`活图`、`指纹`、`节点们`）复用 `evals/共用/活图断言.py` 里那几个不认领域的函数，它的文档串跟着说清哪半边是通用的。
  - **提示词不再给路径**：改成「回流进我这台机上的领域图」，取活图路径这一步由模型自己走；走岔了（写进工作区、写进包内出厂种子）断言都报红。
  - **`evals/用例/回流/断言.py` 改读活图那份**，并新增一条正面的「包内出厂种子一个字节没动」：整个领域目录 22 件逐文件比（不是只比 `领域图.json`），期望值取基线里那份指纹，「活图是种子的逐字副本」这个前提由 `tests/loo0ng-domain/test_seeds.py` 的 `test_活图是包内出厂种子的一份拷贝` 钉着。这一条是本票新加的：提示词不再点名目标之后，模型自己去找「我这台机上的领域图」，找到包内种子是一种真实的走岔法，正面守一道。「没往工作区乱写」那条的期望从三样变成只有 `案件/`。
  - **`tests/loo0ng-domain/test_seeds.py` 的「回流种子」跟着改**：`LOO0NG_HOME` 指进临时目录，三条建在「工作区根上有一份 `领域图.json`」上的断言改成活图那份，另加一条「活图落在临时的活图家里而不是用户主目录」。
  - **种子的 `状态.md` 同改**：摊子从三样变成两样加一份活图，并写明案件那一层的 `AGENTS.md` 指针仍指着包内出厂种子（本种子不改它），模型照它去写会撞上引擎那条无条件拒。
  - **`docs/agents/skills.md` 的 `evals/` 目录段跟着同步**：`共用/活图断言.py` 不再只供四个逐节点用例，`种子/` 那条先跑 `home` 的名单多一个「回流」。

  **两侧各实跑绿**：Claude Code 侧三次（23 回合 118 秒、20 回合 91 秒、19 回合 87 秒），Codex 侧两次（15 回合 127 秒、14 回合 116 秒）；末一轮两侧都是在接完评审之后的这份树上跑的。#105 的风险「Codex 沙箱写不写得动活图家」实跑确认：默认 `workspace-write` 下模型在 `%TEMP%` 的活图家里写得动，不用改沙箱。回合数比 #102 之前那一版（Claude 16 回合）多出来的就是取活图路径这一步，`回合上限` 从 30 提到 40（票里只授权了跟着调 `超时秒`，但 23 回合顶着 30 没有余量，一并调了），`超时秒` 留在 480。Codex 那一跑把 uv 的缓存落成了不带点的 `uv-cache`（此前只见过 `.uv-cache`），`_is_harness_noise` 因此把 `uv-` 开头的一并当 harness 噪音忽略；这个小函数全仓七份逐字相同，七份一起改，不让其中一份走偏。

  **关票触发那条路跟着补一道**：提示词不再给路径之后，手工触发时**模型自己**会跑 `sketch.py home`，而 `--materialize` 生出来的工作区不设 `LOO0NG_HOME`（回放自己兜底只管回放那几条命令，管不到 harness 里的模型），于是 `home` 解析到真的 `~/.loo0ng/`。这一条是关票触发时在 Codex 侧实测到的：一次触发就把一个合成节点写进了开发者自己那份活图。现在 `--materialize` 在活图落于工作区里的种子上多回显一行 `LOO0NG_HOME=<工作区>/.活图家` 与一句为什么要设（两条断言钉着：该回显的回显、不该回显的不多一行），`docs/agents/skills.md` 的关票触发配方两侧都加上设这个变量，那一节里「两条路手工触发时都碰不到律师真的 `~/.loo0ng/`」这句已经是假话，改写成实话。

  **并入一件小的（#105 的评论）：`scripts/skill-eval.py` 加 `--model` / `--effort` 透传。** 从前跑器没有模型参数，两侧都吃各自 CLI 的默认（Codex 读 `~/.codex/config.toml`），想换模型跑只能临时改 `build_command`、跑完 `git checkout --` 还原。现在 Claude Code 侧直接加 `--model`/`--effort`，Codex 侧加 `-m` 与一条 `-c model_reasoning_effort="<档>"`；两个参数都不给时命令与从前逐字相同，这是既有跑法的兼容线（`tests/skill-eval/test_skill_eval.py` 两条断言钉着）。跑器回显第一行报出这次用的模型与档，否则跨跑比较的结果读不出是哪个模型跑的。`--materialize` 那一路照旧一个跑用例才有意义的参数都不收。命令写进 `docs/agents/skills.md`。

- 1664b27: `ask-loo0ng` 把「去领域图查时限」写进死顺序（本轮全量新发现的红）。Claude Code 侧 `两份同时待确认` 四次红两次：红的两次都是 5 回合、绿的两次是 7 与 11 回合，差别在有没有打开领域图；红的那次还自报了一句「领域图在本次会话里读不到」，是猜的，不是取了路径试过。缺口在正文：「我怎么答三问」只写了「案件图里没有的，按顶层领域名读领域图」，而时限只住领域图（ADR-0016），案件图里已经有的节点也得回领域图查一次，这一次查没有被写进任何一步；「往下的路」里那句「节点在领域图上有时限句的，再原样附一行」是条件句，模型没打开领域图就走不到它。与 #65 那三件同一族：要求写在了模型走不到的地方。改法两处：定位段改成每次都打开领域图，写明它有两个用途（问 3 与问 4 的比对、时限句），并点明案件图里已有的节点也要回去查；时限段补一步死顺序，写明写「往下的路」之前逐个拿 `id` 取时限属性，没打开过领域图就断定「没有时限」或「读不到」算没答，只有按 `AGENTS.md` 里的路径真打不开时才说读不到、并写出试过的路径。用例与断言一个字没改。
- e182636: `ask-loo0ng` 的领域图改从活图取（#92，ADR-0019 的收尾）。**核下来路由这一侧是零改动，正文一个字没动。** 正文里领域图出现的四处，没有任何一处自己拼路径：定位段写明路径在工作区根 `AGENTS.md` 里、是起手落下的；五问树的问 3 与问 4 只说「领域图」，整段不碰它在哪；查时限那一步认的是同一条路径（读不到只有真按 `AGENTS.md` 取了路径试过才算数，#71 写硬的那条）。取路径只有这一步，所以上游 #90 把那一行改成指活图之后，路由跟着就读活图了。正文里也没有「领域图随 skill 包内置」这类与 ADR-0019 打架的措辞，没有可改的。

  零改动这件事从此有断言守着，不再靠下一个人重新核一遍。新增 eval 用例「活图多一件」与同名种子：空图起手，模块「申请确认债权」里领域图带入的两件都已确认，活图上还多出第三件「裁定确认职工债权的申请」，带一句只住活图的时限。**那一改落在最后一次写案件图之后**，所以工作区两份视图的「前方」里没有它：模型光读 `图视图.md` 答不出来，只有真按 `AGENTS.md` 那一行打开活图的 `领域图.json` 才拿得到。读包内出厂种子那条路会在这个模块上落空、掉到问 4 答别的模块，用例立刻红。同一条用例顺带核了 #71 那条死顺序（写「往下的路」之前逐个查时限）在活图上照样成立：那句时限只在活图上有。脚本层另加 `tests/ask-loo0ng/test_live_domain.py`，把那四处逐处变成会红的断言（不写死包内路径、不把领域图说成随包分发、定位段与查时限那一步各自点名 `AGENTS.md`、五问树那两行不自己讲路径）。`回放助手.py` 多一条 `用活图()`，把领域目录从包内种子换成一份活图，默认值不变。

  既有工作区那一条核过三种形状，路由的行为都正确：**迁移过的**（手改了 `AGENTS.md` 那一行）指活图，与新起手的工作区没有区别；**没迁移、安装目录不带版本号的**那一行仍打得开、读到的是包内种子，路由照常答，只是看不见律师累计的东西，这个形状本来就由另外六个路由种子覆盖着（它们的领域目录直接给包内种子）；**没迁移、安装目录带版本号的**那一行升级后失效，路由照正文说读不到、并写出试过的那个路径。第三种形状下律师只拿到「读不到」、拿不到下一步（前方与时限都算不出来，而五问树会一路掉到问 5 报「无事可做」，那是假话），这是本次核出来、**没有在本票里改**的一处缺口：改它要动正文与五问树，越过本票的边界，另开票。五问决策树、入口表、时限的呈现方式一个字没动。

- 45eb921: 起手取活图路径改成机械的：`init` 自己调 `home`（#97，ADR-0019）。

  #90 把领域目录搬出 skill 包之后，起手取活图路径这一步是**靠正文驱动的两条命令**：模型先跑 `sketch.py home --name <领域名>` 拿路径，再把它给 `setup.py init --domain`。跳掉第一条的后果正是 #90 存在的理由本身：指针块记下包内出厂种子的路径，律师此后累计在领域目录上的东西下一次升级照样被抹掉；而起手一案一次（ADR-0007），要改只能把工作区整个删掉重来。这是 #65 那一族的同一句教训：**要求写在了模型走不到的地方**。

  本票在票里给的两条路中取**路 A**：`init` 自己调 `home`，正文里那一步彻底消失，模型没有可跳过的东西。路 B（对包内种子路径直接拒、留 `--allow-seed` 逃生口）改动面更小，但它把这一步仍留在正文里，只是把跳过的后果从"事后一句提醒"改成"事前一次拒绝"；而且它会与 ADR-0020「起手那一头不动，`setup.py` 对包内种子仍只报不拒」当面撞上。路 A 不碰那一条。

  `setup.py init` 多一个 `--domain-name <领域名>`：律师侧只打一条命令（`init --full --domain-name 破产`），活图目录由 `setup.py` 子进程调 skill "loo0ng-domain" 的 `sketch.py home` 取回，`home` 的回显原样带给律师（首次起手拷了几件看得见）。这与它子进程调图引擎是同一个形状：活图仍归 `loo0ng-domain` 管，`setup.py` 只是调用者，不自己算活图在哪、也不自己拷种子。`--domain <路径>` 原样保留给开发侧（种子回放、开发者手上那份定制的领域目录），两者二选一、同时给即拒。

  `home` 拒（这个领域既没有活图、包里也没有种子）时起手跟着拒，发生在建格与落图之前：**一格不建、一个字不写**，律师原地改成空图起手（`--empty --name <领域名>`）即可。`--sketch` 与既有的 `--engine` 同一个用途，给装在别处时指路。

  领域名不要给两遍：`--domain-name` 与 `--name` 同给即拒（两个给成不一样的，指针块的「领域」与「领域目录」会各指一处）。三选一的形状一个字没动：定制图自带领域名，`--domain-name` 对它仍是可选的。

  `tests/loo0ng-setup-case/test_setup.py` 新增一组（`一条命令Case`）：一条命令起手后指针块记的仍是活图、首次起手经这一条命令就把出厂种子的三样拷出来、两种传法同时给即拒、取不到活图时一格不建一个字不写、给了 `--domain` 就根本不去碰活图家、定制图也能只给领域名、领域名给两遍即拒，外加一条钉两端回显契约的（`活图：` 这个前缀两件 skill 各持一份字面量，互不 import）。首次拷种子那一条用 `--sketch` 指一份合成的假包，真的 `assets/` 一个字节不碰（ADR-0015）。`loo0ng-setup-case` 与 `loo0ng-domain` 的正文、用例「起手」的说明、`docs/交付/现场清单.md` 段 3.4 跟着改；`sketch.py home` 本身与活图的位置一个字没动（票的边界）。

## 0.1.0

### Minor Changes

- 新增参考 skill `loo0ng-graph`（#26）：图引擎 CLI（`scripts/graph.py`，标准库零依赖），`图.json` 的唯一写入口。起手三选一、构成操作（节点增改序移与空白模板、模块增改名序与删空模块）、条目只追加（生成 / 确认 / 不适用，来源由动作推出）、状态只算不存、模块级不适用展开、严格惰性带入、引擎拒写（已确认不能不适用、不合校验拒写、原子写）；每次写图后覆盖重算 `图视图.md` 与 `图视图.json`（来源、前方、时限按 id 从领域图查出，两份 JSON 共用格式版本 1）。`references/格式.md` 定稿两份 JSON 的字段表与最小样例。另交付合成小领域 `evals/领域/菜园/` 与种子「图引擎」、用例「改标题」。
- 新增参考 skill `loo0ng-filing`（#27）：两个零依赖 CLI，互不 import，也不 import 图引擎。`scripts/archive.py` 归档搬运：收件箱内相对路径原样搬到材料、指南、模板/官方、模板/生成，不改名、不覆盖、不越界、不合并目录，同名已存在则那件不搬并报出，压缩包与照片只能原样归材料，永不写入 `材料/律师陈述/`，不写图。`scripts/statement.py` 陈述落档：一条一文件、落盘后只读，文件名 `YYYYMMDD-HHMMSS-<标题>.md`，头部六字段顺序固定，正文原话逐字、多值加 agent 整理表，裁定固定问题与裁定两段，转述必填来源，取代目标须在同目录，拒绝覆盖。`SKILL.md` 写明三个归档进入点与「不在盘上的事实不是材料」；`references/格式.md` 定稿陈述文件格式与归档规则表。另交付种子「收件箱」与用例「归档一件」「记一句」。
- 新增参考 skill `loo0ng-to-docx`（#28）：两个 CLI，互不 import。`scripts/md2docx.py` 冻结转换器只依赖 python-docx、离线：输入 Markdown 最小集（一级标题、正文段、`:left:` 顶格、`:right:` 右对齐、管道表、空行分隔），打开该节点的官方模板、清 body 只留 sectPr、段落格式与字体取自模板里现成的段落，第 n 张管道表照抄模板第 n 张表的 tblPr、列宽、每行行高与格格式，合并单元格用 `<`（并左）`^`（并上）约定，模板里没有对应的表就出等宽自由表；最小集之外的写法与表形不合一律拒绝；收尾清元数据、正文以表格收尾补一个空段；默认写到 `%TEMP%/loo0ng-to-docx/`。`scripts/gate.py` 门禁只读、可单独对任意 DOCX 跑（依赖与结论值域随后由 #62 改写）：不通过项有行高与页数阈值、空白页、表宽超页面、页脚 PAGE 域写死或丢失、表格直接接 sectPr、元数据残留、模板占位与说明段残留；披露项有空单元格坐标、请求字体不在嵌入字体里、页数、有字没渲出来；`--deliver` 通过才一次性拷进工作区且不覆盖。`references/最小集.md` 与 `references/审查报告.md` 定稿最小集、合并约定与审查报告五段格式。跑器 `用例.json` 新增可选键 `Codex沙箱`（Codex 沙箱里起不来 Word COM）。另交付种子「模板」与用例「出一版」。
- 新增参考 skill `loo0ng-domain`（#29）：领域目录与雏形机制。`assets/破产/` 下领域目录三样就位：19 件官方模板原件（自 `knowledge/模板/` 搬入）、2 份指引手册原文（自 `docs/指引手册/` 搬入）、领域图「破产」的合法空壳（有领域名、无模块，模块与节点由下一票对指引手册跑雏形长出）；既有案件的 `.doc` 不进。`scripts/sketch.py` 零依赖、不 import 图引擎：`check` 按标题判重（归一化后同名的已在图里不重复提出、相似不同名的列为待定、与领域图同名的标为带入）并回显清单、图一字不动；`apply` 在律师一句话拍板后逐条经 skill "loo0ng-graph" 的引擎子进程写入，待定未定即整个拒，案件图上时限句只回显不写；`from-case` 是回流第一步，从案件图算出「领域图没有且至少一版已确认」的模块与节点、保留原 id 写成雏形文件；`docx-text` 把 docx 指南与手册打成纯文本供整读。`references/` 三篇：雏形格式与判重规则、时限句取舍、回流步骤。另交付种子「指南」与用例「出雏形」，`tests/loo0ng-domain/` 用合成小领域跑。
- 领域图「破产」（#30）：对领域目录里两份通用指引手册（《破产管理人实战手册（2024 年版）》《破产案件各流程节点管理人操作指引及申请类文书模板（2024 年版）》）跑一次雏形机制，把 `skills/loo0ng-domain/assets/破产/领域图.json` 从空壳长成 12 个模块、72 个节点。节点粒度按 `CONTEXT.md`「节点」词条定：一个节点恰是一份文书，同时递交的多份是同一模块下的多个节点，实物动作（接管、张贴、签收）不做节点、只在某份文书里被载明；这一条把 `knowledge/流程模型/破产.md` 的 53 个「办案步骤」节点重切成 72 个「一份文书」节点。19 件官方模板每件恰好挂在一个节点上、其余节点空白模板为「无」；24 个节点带一句话时限（ADR-0016：管理人自己要守的、有天数或有可在图上核对的锚点的期限，去案件化原文加括注出处与性质；「及时」「尽快」、管理人告知他人守的期限、本案具体日期都不记，一条规则一个出处括注，第二个只留给两源冲突）。id 由作者给成稳定可读的 ASCII（`m-` / `n-` 前缀），回流与惰性带入都按它对齐。写入全程经雏形 CLI `check` / `apply --kind domain` 转图引擎，未手改 JSON。`tests/loo0ng-domain/test_assets.py` 随之从「空壳」改为验收：合校验、id 唯一、无条目、标题各落在一份文书上、19 件模板各挂一次、时限句一行且出处括注不超过两个、以整份领域图起手的测试工作区前方为空且每个节点来源为领域图、领域图 JSON 一并过隐私扫描。
- 新增编排 skill `loo0ng-setup-case`（user-invoked 双旗，#31）：律师在一个空目录里打一句起手。`scripts/setup.py` 零依赖、不 import 图引擎，两个子命令。`init` 的前置是当前目录没有 `图.json`（一案一次，ADR-0007），顺序是先落图再建格，引擎拒了就一格不建：起手图三选一（`--empty` / `--full` / `--from` 转 skill "loo0ng-graph" 的引擎子进程）落下 `图.json` 与两份视图，再建六格（`收件箱/`、`材料/律师陈述/`、`指南/`、`模板/官方/`、`模板/生成/`、`文书/`），按起手图上挂到的官方模板从领域目录拷进 `模板/官方/`（缺原件只报不拒），最后照 `references/工作区AGENTS.md` 写工作区指针块 `AGENTS.md` 与一行 `@AGENTS.md` 的 `CLAUDE.md`（ADR-0009 机制 B：领域名与领域目录绝对路径、图与视图文件名、入口名、只读声明；由起手图机械推出，不加确认点）。`register` 是起手清单里「既有成品登记为已生成、来源律师」那一条的机械落地：按 `references/既有成品审查报告.md` 写一份五段固定的审查报告（每一版文书必有一份），再经引擎追加一条来源为律师的生成条目，引擎拒了就撤回报告；它只登记不确认。`SKILL.md` 写死七步的顺序与唯一那个确认点：建目录 → 起手图 → 归档（skill "loo0ng-filing"）→ `指南/` 非空则跑雏形（skill "loo0ng-domain"，只 `check` 不 `apply`）→ 回显起手清单（雏形加既有成品建议登记，合成一张）→ 律师一句话 → 写图，收尾三段同别的入口一个形状。

  另交付起手两场景的种子：「空目录」（还不是工作区，`收件箱/` 里六件合成文件各对着一个归档去向加一件既有成品）与「在办中」（按真实的破产领域目录整份领域图起手、一个已确认、一个已生成未确认、两条律师陈述），并把种子「图引擎」的回放从自己写指针块改为调真的起手 CLI（另三个种子与四个用例的断言随之改）。新增用例「起手」，`tests/loo0ng-setup-case/` 三个文件跑脚本层、依赖边界与两条种子的回放。

- 新增编排 skill `loo0ng-doit`（办节点，user-invoked 双旗，#32）：律师在案件工作区打一句自由文本 = 动作 + 节点 + 补充，一个对话只服务一个节点（ADR-0008、ADR-0010）。拍板与兜底先行，主循环不靠生成就能走通：「确认 X」「X 不适用」「模块 Y 不适用」经图引擎落一条原样存着律师那句话的条目，三条留痕规则（原话原样存、只在律师明确说出且指向本节点时落、同一对话第二次出一版指向另一节点即拒绝）替代入口门；「X 我自己写好了」先归档，对 DOCX 跑一次只披露不阻断的门禁，按 `references/兜底件审查报告.md` 写报告，再登记为已生成、来源律师，文书一个字不改。出一版的顺序定死：开场归档、复制律师指的文件（别案成品剔除案件内容后进 `模板/生成/`）、惰性建节点、材料指南空白模板三样全无则只回缺什么且不算生成失败、整读、写 Markdown 最小集、转换、门禁、落盘 `文书/<节点标题>/` 三件、追加带三条相对路径的生成条目、回显审查报告要点与收尾两行；门禁不过改稿重转至多 2 次，仍不过就生成失败、不落盘、不追加条目。生成不问缺事实，缺项进审查报告的待律师裁定，律师在对话里答一句先落成陈述（`--reply` 指向那份报告）再重出，仍在同一回合。本 skill 没有自己的脚本，图、归档与陈述、转换与门禁分别经 skill "loo0ng-graph"、skill "loo0ng-filing"、skill "loo0ng-to-docx" 的 CLI 落盘。#28 真实触发暴露的两处空白一并定死并写进两件 skill 的正文：落进 `文书/<节点标题>/` 的就是一次生成（三件加一条条目，缺一即生成失败），律师只说「转成 word」「检一下」时不落进 `文书/`、不写审查报告、不动图；生成条目只有本 skill 一条来路，参考 skill 被够到之后不自行追加条目、不自己补写审查报告。另交付种子「兜底」与五个用例（拍板、模块不适用、换节点被拒、兜底登记、办节点出一版），`tests/loo0ng-doit/test_seeds.py` 验种子状态；eval 跑器删临时工作区改为退避重试（门禁起的 Word 会多攥一会儿刚检过的件）。
- 新增路由 `ask-loo0ng`（问路，user-invoked 双旗，#33）：律师随时可打的只读地图，可选、无状态、不触发、不写图、不替人按下去，前置只是案件工作区里有 `图.json`，没有任何入口依赖它（ADR-0005）。读图定位：读 `图.json`，案件图里没有的按顶层「领域」名读领域图（绝对路径取自起手落在工作区根的 `AGENTS.md`），两张图靠 id 对应，标题被改过也不丢；当前节点 = 已生成待确认（可复数，一个都没有就答「没有待确认的文书」），上一完成 = 仍处于已确认里确认最晚的（重出过的不算），下一任务先列全部待确认再由五问有序决策树定一个要生成的，每次标出命中哪一问，长理由外置到 `references/下一任务.md`。时限不进决策树，只把领域图上那句话原样附在「往下的路」里（ADR-0016）。回复形状照 ask-matt：你在哪（主线四步）、三问、往下的路（每步标出拍板点与要不要开新对话）、相近入口分界线、下一句该打什么（`/名 …` 与 `$名 …` 两个平台各一行，`$` 打裸名不带命名空间前缀），然后停。自持三个入口的 ASCII 表、不读目录，正文写明「入口都存在，即使不在你看到的 skill 列表里」与「凡对入口行为的断言以对方 `SKILL.md` 为准」；图里对不上的事不猜、不造入口、不新建节点，说对不上并指向 `loo0ng-doit` 后照常答三问。另交付六个种子（空图、两份待确认、跳着走、整块不走、重出、自加节点）与七个用例（七段情形各一条），`evals/共用/` 收三个共用模块：种子回放动作、只读基线（三份图文件的 sha256，跑完比字节）、ask-matt 五条验收项加「只指向表里的三个入口」；`tests/ask-loo0ng/test_入口表.py` 把「改任何入口必改这张表」这条结构不变量变成会红的断言。另两处约定随本票补齐：`AGENTS.md` 结构不变量 1 写上路由是 `loo0ng-` 前缀的例外（它每轮进上下文，写着的规则不能是假的）；`docs/agents/skills.md` 写明路由正文里的入口名是律师要打的那一串、只能裸着写，不套「调用 skill」句式。
- 版式门禁改成三值、推算层升为本体、渲染层降为机会性加信（#62，落 ADR-0017）：`scripts/gate.py` 的 `import pymupdf` 从模块顶层挪进渲染分支，门禁本体从此**零第三方依赖、且不新增任何依赖**（静态检查加 CJK 版面推算，只用 `zipfile` / `xml.etree` / `re` / `collections`），没装 PyMuPDF 也跑得动；推算器只读 DOCX 里写死的几何，不打开任何字体文件，为 `vMerge` 续格建带（不并带则四件合并单元格模板行高低估 34%～ 51%），正文段落按行拆页（整段挪页会每段浪费一次，长文书系统性多算页数）。`CannotRun` 里四处「无渲染后端」全部删掉，渲染层失败即跳过、只写一条披露项，无渲染时请求字体与有字没渲出来两条披露项整条消失、不报「无」；新增 `--no-render` 显式走这条跑道。结论值域改三档：推算给出 `[lo, hi]`、阈值 `T`，`T < lo` 不通过、`T >= hi` 通过、`lo <= T < hi` 需人眼，整件取最重的一档，需人眼只可能由页数、空白页、最大行高三项产生；有渲染结果时点值取代区间、这一档当场坍缩，点值落在区间外时结论以点值为准并出固定名披露项 `推算区间不含实测值`（同时进审查报告与 stderr）。退出码改为 0 通过 / 3 需人眼 / 1 不通过 / 2 门禁跑不动，落盘被拒从 1 挪到 2；`--deliver` 在通过与需人眼两档都落盘，位置相同、文件名不加装饰；重试只数不通过。结论为需人眼时门禁另出一段**须目验清单**（每行带项名、坐标、推算区间与阈值、最坏越界幅度，末尾一句「确认前请在 WPS 或 Word 里打开看这几处」），由门禁生成、模型不撰写，格式住进 `references/审查报告.md`。测试跟着分层：`tests/loo0ng-to-docx/` 每个用例现在跑无渲染与接渲染两条跑道，推算层那条在任何机器上必须全绿、不许 skip，渲染层那条缺渲染通道时 skip 并打印一行说明（ADR-0015 的口径随门禁本体改）；六行数值断言与两个魔数（45 空段、59 段）换成探针式，断言只断结论类别与项名；`test_no_backend_is_exit_2_not_a_downgrade` 反向重写成「无渲染后端照常给结论」；新增 `test_layout_estimate.py` 与三件推算回归件（行高落在 #55 没证到的 120 ～ 400 磅一段、页数正打默认的 30 页阈值、空白页在页边界收尾的一对），真值造件时 Word 与 WPS 各量一次冻进测试文件、之后零依赖，另钉住「推算全程不打开任何文件、结果可复现」；新增一件带内 fixture 钉住需人眼这一档，且 `assert_pass` 不得接受该档。对外话术从「零依赖」改口为「门禁本体零第三方依赖，且不新增任何依赖；PyMuPDF 降为可选，只在有渲染结果时才 import」，`SKILL.md`、两处 references、`README.md`、changeset 与 skill `loo0ng-doit` 的出一版一段同步。
- `loo0ng-to-docx` 的运行环境改为 agent 自备，约束的对象从「哪个解释器」换成「转换器的后端版本」（#75，ADR-0018）。起因是律师 2026-09-08 的裁定：各用户机环境各不相同，很多机器完全没有 python，写死限定等于工作台永远出不了件，而 agent 有能力自己完成初始化的配置；这是 ADR-0017「缺 Word / WPS 不得阻断出件」那条原则往上游挪了一层，理由更硬（没有渲染器只是少一层加信，没有能跑转换器的环境是一步都走不动）。`SKILL.md` 里「两个都用系统的 python；`uv run python` 不用」那句删掉，换成新的一段「跑得动转换器的环境」：怎么弄到解释器归 agent，只写四条约束（能 `import docx` 且是钉住的那个版本；装出来的东西只落在临时位置，不进案件工作区、不进 skill 目录与本仓库、不进用户全局环境；不静默；门禁本体零依赖照旧，转换器起不来时已有的 DOCX 照样检得动），另给一条 2026-09-08 在 Codex Full access 沙箱里真走通过的路（`uv python find` → `uv pip install --python <它> --target <临时目录> -r requirements.txt` → `PYTHONPATH` 指过去），并写明它只是一条路不是唯一路。受限沙箱与律师那台 mac 上能不能自备环境**没验过**，正文里如实写着未验并指向 #78、#79，不得当成已验。

  顺带补上一个真洞：`python-docx 1.2.0` 此前只活在散文里（ADR-0017 与两份 changeset），仓库里没有依赖声明、没有任何机械检查，1.3 一发布自备环境的 agent 就会静默换掉「冻结转换器」的后端。现在钉子落在 `skills/loo0ng-to-docx/requirements.txt`：精确钉 `python-docx==1.2.0`（不用区间，转换器扎在 `docx.oxml` 那一层，上游对它不作 SemVer 承诺），注释里写明 python 3.9 底线的出处（律师那台 mac 是 3.9.6，底线由 #57 审定，#61 是栽在它上面的那次）与「已知可跑范围」今天只有 1.2.0 一个数。清单放在 skill 目录顶层而不是仓库根：两条安装路都只带 skill 目录，根上的清单到不了律师机（skills.sh 的拷贝器 v1.5.24 只排除 `metadata.json` 与 `.git`/`__pycache__`，插件是整目录订阅，清单两边都到得了）。

  **版本对不上照跑，但必报**：转换器不因版本不符阻断出件，退出码照旧；stdout 第一行仍是「已写出 <路径>」，新增固定的第二行「出件环境：解释器 …（python x.y.z）；python-docx <实测版本>（与依赖清单钉的一致 / 依赖清单钉的是 …，这不是钉住的那个版本）」，实测版本问运行时要（`importlib.metadata`）而不是照抄清单常量。改稿指引仍只在 stderr。模型把那一行原样抄进审查报告的「生成依据」段，**恒常写、不只在对不上时写**；`references/审查报告.md` 的段落定义与两份最小样例同步更新，五段的段名与顺序没动。转换器的第三方 import 仍然只有 `docx`，读清单与报版本只用标准库。

  `tests/loo0ng-to-docx/test_requirements.py` 是这颗钉子的机械守门，五条断言一条都不许 skip（ADR-0015：依赖钉不是渲染层，它红的那一刻正是它唯一有价值的时刻）：清单形状是精确钉且只此一条依赖；本机实装版本等于清单里的数；环境那一行恒常写且位置固定在第二行；拿替身包元数据跑一次验证回显里的版本取自运行时、且版本对不上时照常出件退出码为 0 并明写这不是钉住的那个；清单里的数与散文里写出它的每一处（`SKILL.md`、`references/审查报告.md` 的两份样例、`docs/agents/skills.md` 的布局表、ADR-0018）一致，散文里再多一份副本就红。`docs/agents/skills.md` 的 skill 目录布局表补上 `requirements.txt` 这一项（该表此前是封闭的五项）。门禁本体、阈值、三档结论、Markdown 最小集与表形规则一个字没动。

### Patch Changes

- Codex `$` 补全里的显示名改为 skill 名：四件 `SKILL.md` 的 `metadata.display-name` 改成各自的 `name`，`agents/openai.yaml` 重生成；生成器加一条校验，`display-name` 不等于目录名即报错（ADR-0009 附注，#29 真实触发时发现律师按 `loo0ng-` 名字找不到中文显示名）。短描述保留中文。
- 回流有了验收层（#34，ADR-0012）：机制本身（`sketch.py from-case` 与 `references/回流.md`）随 #30 已在，这一票只补验证，`skills/` 下除 `references/回流.md` 多一行「跑两侧的用例「回流」」外没有行为改动，所以是 patch；真正的一次回流改的是 `assets/<领域>/领域图.json`，那一次才是 minor。新增种子「回流」：借跑器的 `replay_seed` 把种子「在办中」摊进 `案件/`，再加两个律师自加节点（一个已确认、一个只生成，标题都带着本案的当事人与日期），工作区根另有破产领域图的可写副本与一份 `基线.json`（案件图与领域图的 sha256、领域图的模块数与节点数）；副本而不是 `skills/` 下那份原件，一次 eval 不该动到仓库里的领域图（ADR-0015），基线让断言不必硬写「72 个节点」。新增用例「回流」：开发者一句话给案件工作区的路径，两侧都是裸提示词，断言回显清单标出了去案件化后的标题、领域图恰好多出那一个节点且排在模块末尾、id 与案件图一致、条目为空、只生成没拍板的那个没回流、案件图字节不变、雏形文件没落进工作区。`tests/loo0ng-domain/test_seeds.py` 复核种子状态与 `from-case` 恰好提出已确认的那一个；`test_sketch.py` 加一条「`from-case` 与 `check --kind domain` 两张图的字节都不变」，即拍板前领域图一字不动；`test_isolation.py` 加一条会话边界，律师面对的三个入口（含还没建的 `ask-loo0ng`）的正文里出现「回流」即报红。分支加 PR 那一道不进 eval：隐私钩子在 `main` 上守 `loo0ng-domain/assets/` 下的图由 `tests/privacy-check` 覆盖，第二双眼是另一会话的 AFK 代理审 diff。
- `ask-loo0ng` 的「打法」段补一句律师实际怎么按（#44）：Codex 客户端的补全按子串匹配，打 `doit` 就切到 `loo0ng-doit`、打 `ask` 就切到 `ask-loo0ng`，再按 tab 选中，不必记 `loo0ng-` 前缀；只打一个 `$` 反而不好找，列表二十几项、这几件不在首屏（#40 实测）。原有那句「整串、裸名、不带命名空间前缀」保留不动，路由输出的五段形状与「下一句该打什么」给整串的规矩都没变。这一条只在 Codex 客户端实测过，正文里写明 Claude Code 的 `/` 补全没测过、别照搬。
- 图引擎与雏形在 python 3.9 上写得成图（#61）：`Path.write_text` 的 `newline=` 是 3.10 才加的，而律师那台 mac 的 `/usr/bin/python3` 是 3.9.6，于是唯一写图路径 `write_text_atomic` 每次都抛 TypeError，起手、追加条目、改构成、雏形拍板全部写不成图。`graph.py` 与 `sketch.py` 两处改成显式 `open(..., newline=)`，`newline` 的语义（不让 Windows 把换行翻成 CRLF）一字不变，行为无改动，所以是 patch。同因的第三处在票外：`evals/种子/回流/回放.py` 不随包分发，但 `replay_seed` 用跑测试的那个解释器起它，#61 的验收里 `tests/loo0ng-domain/test_seeds.py` 就卡在它上面，一并改了。新增 `tests/python-floor/`：按 3.9 的 feature_version 解析这两批文件、再按名字拦 `write_text/read_text` 的 `newline=`，让这条版本地板不必靠人记；别的 3.10 API 等真栽了再加（ADR-0015：事故先红后绿）。`docs/agents/skills.md` 的「命名与编码」段随之多一行。
- Codex 侧 eval 三件红，改的都是正文（#65）。三件全在验回复的形状，没有一件是写图或脚本失败，Claude Code 侧同批全绿；共同的毛病是要求写在了模型走不到的地方，或写得不够硬。`loo0ng-doit`：「同一对话第二次出一版指向另一节点即拒绝」原先只住在「三条留痕规则」列表里（谈确认与不适用的那一节），而「出一版」的死顺序里没有它，模型照顺序走就走不到，两次跑都只答了「三样全无、缺什么」；现在把「换节点检查」提成死顺序的第 3 步、排在惰性建节点之前，写清一句话里连点两个节点时前一个照常走完、后一个在这里被拒，并给出开新对话那两行，写明被拒的那个节点不在这个对话里算它缺什么（要算就得读它的模板与材料，那已经是在办它了），原第 3 至 7 步顺次改成第 4 至 8 步；第 5 步「三样全无」的收尾话术原先是「下一句仍是在这个对话里继续办这个节点」，与拒绝直接冲突，补一句限定说明这句只说给本对话正在办的那一个。`loo0ng-setup-case`：收尾第一段原先是一句话列五样，两次跑都只报了新增节点与归档件数、漏掉六格与两份视图，现在做成五行骨架（六格与图、起手图、归档、雏形、既有成品），每行起头几个字原样照抄，少一行就是没照做。`ask-loo0ng`：五个二级标题的要求原先与别的排版建议同段，抖动那次一个 `##` 都没有还加了收尾，现在单独一段写硬，写明不带这五个二级标题的回复算没回、发出去之前先数一遍（ADR-0015：抖动是正文问题，不用多数决盖住）。用例侧只动一处断言：`起手` 的 `check_收尾三段` 原先笼统查「六格|图.json|图视图」一个词的存在，拆成按正文那五行逐样查的三条（第一段逐样、第二段拍板、第三段下一句），报红时指名漏了哪一样。
- 关票触发改在从种子生出的临时工作区里做（#66）。原先的做法是在 Cases 根下常驻两个合成工作区、每次关票都在它们上面触发，这既不是 ADR-0009 说的真实案件、也不是 ADR-0015 说的只生不存，两头不挨着且没写下过规矩；而触发要改图、改图不可逆（节点永不删、不适用是终态，ADR-0003），留下的痕走支持的路子撤不掉，撤只有标不适用（残渣更多）或手改 JSON（不受支持）两条，攒几轮那两个「现成起点」就既不像刚起手也不像办到一半。改法照票里定的甲案：漂移不被管理而被根除。`scripts/skill-eval.py` 加 `--materialize <种子名>`，生一个回放了该种子的工作区、打印路径就停，不跑 harness 也不删（生出来是给人接着用的），`--harness` 因此改为非必需，跑用例才有意义的参数（`--harness`、`--case`、`--runs`、`--keep`、`--max-turns`、`--timeout`）与它一并互斥，给了就报错说清是哪个，不静默吃掉；`tests/skill-eval/` 加七条，旧的 `test_harness_is_required` 名字已失真、断言与新那条重复，删掉。ADR-0009 的完成定义随之改口径（关票触发以临时工作区为准，真实案件不再是关票的前提，其余不变），ADR-0015 把「只生不存」的适用范围延伸到关票触发，两处各写一条同日附注。`docs/agents/skills.md` 补「关票触发怎么做」一段：挑哪个种子、一条命令生工作区、Claude Code 侧那行可照抄的 `claude -p`、Codex 侧只能人工（`$名` 在 AFK 下不解析，`codex debug prompt-input` 可复核）、用完删、路径不进 issue。`CONTEXT.md`「测试工作区」词条补「关票的真实触发也在其中做」。两处局限照实写进 `docs/agents/skills.md`：Claude Code 侧那行触发命令与跑器给这一侧拼的是同一条，两道门在这一侧几乎重合，增量大的是 Codex 侧（那边 eval 用替身提示词、根本没测触发）；关票评论里「验到什么」的话术也改准了：验到的是这条安装路线下按名字触发（Claude Code 侧 junction 裸名、Codex 侧客户端带前缀名），插件路线的 `/loo0ng-skills:<名>` 不在此列。Cases 根下那几个常驻合成目录按律师的话留着，但不再承担任何门槛，只作手工探索的起点。
- 需人眼那一档有了 eval 覆盖（#73，ADR-0017）。门禁结论三档上台后（#62），脚本层钉住了门禁自己在这一档的行为（`tests/loo0ng-to-docx` 的 `ThreeValuedConclusion` 四件），门禁之外那一段没人钉：编排 skill 拿到退出码 3 之后做什么。`skills/` 一个字没改，所以是 patch。新增种子「带内」：在种子「在办中」之上，把待办节点「管理人银行账户备案报告」挂着的官方模板 `1-3` 第一张表第一行的 `trHeight` 钉成 `hRule="exact"`、`val="4000"`（= 200 磅），由回放脚本从工作区里那件模板原件现做、原地换掉，仓库里不放二进制。`hRule="exact"` 的行高与格里写什么无关，推算层直接取这个数，所以只要成品里有那张表，最大行高的推算区间就恒为 196.0 至 206.0 磅，正好把默认阈值夹在中间，一次由模型自由写正文的生成落在带内而不通过项为空，这一档于是干净地单独可测。只钉第一行不钉整表：整表钉满会把文书顶到两三页，页数与空白页两项跟着变量多。新增用例「需人眼落盘」：断言退出码 3 不被当成生成失败（三件照常落进 `文书/<节点标题>/`、文件名不加装饰）、须目验清单原样在审查报告第一段且每行带项名与坐标、图上多一条来源 agent 的生成条目、只落一版（需人眼不触发重试）、没有节点被自动确认，另有一条守着种子的前提（成品里得有模板那张表，否则钉子落空、门禁给的是通过）。按 #59 只断结论类别与项名，不断数值。一处与票面不同：票里记的「本机实测退出码 3」只在无渲染跑道上成立，开发机装着 Word 与 PyMuPDF，同一件带渲染跑量到点值 200.1 磅、结论是不通过（有渲染结果时点值取代推算区间，需人眼当场坍缩）。这不是种子的毛病，是律师那台机器（mac + WPS）的常态跑道，所以提示词里加一句「这台机器上没装 Word」，让 skill 走 `skills/loo0ng-to-docx/SKILL.md`「没有渲染后端」那一节写明的显式跑道（`--no-render`），断言重跑门禁时同理；局限照实写进用例说明。`办节点出一版` 的说明里「不通过那一档想得到的两条造法都别扭」已不成立，改口指向 #74（同一套造法，只是把数钉到整个区间都越界）。
- 不通过那一档的收尾也有了 eval 覆盖（#74，ADR-0017），与 #73 一对孪生，`skills/` 一个字没改，所以是 patch。原先兜着这一段的只有 `tests/loo0ng-to-docx` 的故障件 fixture 与门禁自己的 fail-closed；没人验的是门禁之外那一段：编排 skill `loo0ng-doit` 拿到退出码 1 之后做什么、做到第 3 次仍不过时收不收得住。新增种子「越界」：与种子「带内」同一个手术只换一个数，官方模板 `1-3` 第一张表第一行的 `trHeight` 钉成 `hRule="exact"`、`val="5000"`（= 250 磅），推算区间恒为 245.0 至 257.0 磅、整个都在默认阈值 200 磅之外，是干净的不通过、不掺需人眼；带渲染跑同样不通过（实测点值 250.1 磅），两条跑道同一个结论，所以提示词就是律师那句「出一版 X」。这一档要的是**改不好的**失败：行高来自模板、与正文内容无关，模型规规矩矩按不通过项改稿也改不动它，转换器又照常出件（退出码 0）、一定走得到门禁，于是躲开了原先想到的两条别扭造法（骗模型把占位留在稿里、塞一份坏模板半路就退出 1）。手术本身写在「带内」的 `起手并钉` 里、两个种子共用，「越界」的回放只传一个数。新增用例「不通过收尾」：断言全是终态，`文书/<节点标题>/` 一件都没有、连目录都没建，图上没有新的生成条目、没有节点被自动确认，没有审查报告落盘，`模板/生成/` 与 `材料/` 没被塞东西，回复里把门禁原话交给了律师并提了兜底自写。skill 正文那四条明令（不调阈值、不跳门禁、不把临时件手拷进 `文书/`、不改成品 DOCX）全收在「一件都没落盘」这一条里。票里提的「扫回复里有没有 `--max-row-height`」这条没做，实测是假阳：阈值由律师定，把这个杠杆讲给律师听是正文允许的收尾，两次跑里模型都这么收的尾。「至多 2 次」这个数仍断不了，照实写进用例说明：转换器写在 `%TEMP%`，重转的中间件不进工作区。照着一次真回复还看出一件票里没料到的事：这份失败**太好诊断**，模型读得懂行高来自模板的 `hRule="exact"`、与稿子写什么无关，跑完第一次门禁就断定改稿改不掉、另对模板原件复核一次门禁，然后直接收尾交人，**没有真去走那两轮改稿重转**。所以这一对种子与用例钉住的是不通过那一档的**收尾**，钉不住**重转**本身；要覆盖重转，得另造一份「看上去改得动、实际改不动」的件，那是另一张票的事。在那之前那个数仍只有两处正文与人工触发看着，别为了断它去改转换器的落点。「回复里带着门禁那条不通过项的项名」这条断言同时兜住「压根没跑门禁、直接报生成失败」那种走法。`办节点出一版` 说明里那段缺口随之改口，指向这两个用例。
- 受限沙箱（Codex `workspace-write`）下 agent 自备环境**实测成立**，`evals` 里为绕开它而标着全权限的那 11 个用例全部改回默认的 `workspace-write`（#78，ADR-0018 明写的两处未验之一）。此前只在 Full access 下成过一次，跑器因此让凡要跑本仓库 CLI 的 Codex 用例都提权，绕法自 #28 起、#62 复验过一次。

  实测（Codex CLI 0.153.4、uv 0.12.1、Windows 11，同一份探针脚本沙箱内外各跑一次，逐条记退出码）：沙箱里 `where.exe python` / `py` / `python3` 三条全空，这条老观察成立；但根因不是 PATH 而是 **ACL**：LocalAppData 下那个 Python 目录与 WindowsApps 的权限继承是断的，沙箱账户读不到，而 uv 自己与它管的解释器落在 Roaming 与 `~/.local/bin` 下，继承得到沙箱账户的读写权，所以 `uv --version`、`uv python find`、`uv pip install --target <临时目录>`、`PYTHONPATH` 指过去 `import docx`、转换器出件、门禁给结论，退出码与沙箱外逐条相同，产物是合规的 OOXML 包、门禁通过。11 个用例改回默认值后一次全绿，回合数 9 至 18、64 至 235 秒，比原先在全权限下记的还低（`起手` 27 至 38 → 16 至 17，`办节点出一版` 39 至 47 → 14 至 18）：模型在沙箱里直奔 uv，反而不再花回合去试那些读不到的解释器。

  结论是**有条件成立**，三个条件写进了跑器的注释与 `docs/agents/skills.md` 的键说明：uv 得在沙箱账户读得到的地方；至少已有一个 uv 管的解释器（`uv python install` 在沙箱里写不了 uv 的锁文件，装不了新的）；uv 的缓存要指到能写的地方（默认缓存在 LocalAppData 下，沙箱里初始化即拒绝访问）。沙箱能写的只有工作区与 `%TEMP%`，用户主目录与 uv 默认缓存都写不了；网络本身没被拦（uv 冷缓存下真下载了 lxml），拦住的是走 Windows Schannel 的 TLS 客户端（`curl.exe` 报 `SEC_E_NO_CREDENTIALS`，`Invoke-WebRequest` 连接被关闭），uv 自带 TLS 栈所以不受影响。

  `danger-full-access` 这个值留在跑器里不删：它是跑器的能力，不再是任何用例的前提。ADR-0018 的裁定一条没动；`loo0ng-to-docx` 的 `SKILL.md` 只把「受限沙箱与 mac 都尚未验证」那半句改成受限沙箱已验、mac 仍未验（#79）。

## 0.0.0

仓库骨架（#24）：`skills/` 目录、Claude Code 插件与单插件市场清单、Codex 插件清单、`scripts/link-skills.ps1` 幂等 relink、changesets 与本文件、`AGENTS.md` 结构不变量与 `docs/agents/skills.md` 维护段。还没有任何 skill。
